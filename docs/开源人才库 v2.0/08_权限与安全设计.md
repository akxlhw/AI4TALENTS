# 开源人才子系统 - 权限与安全设�?(v2.0.0)

**文档编号**�?8  
**版本**：v2.0.0  
**状�?*：设计稿  
**适用范围**：智能人才库—开源人才子系统 V2.0.0

---

## 1. 设计目标

明确开源人才子系统在身份认证、权限控制、数据隔离、API 安全和审计留痕方面的设计，确保：
- 与学术子系统共享统一 IAM 体系
- 开源数据（`os_*` 表）与学术数据（`core_*` / `raw_*`）完全隔�?- 高危操作（采集配置、任务触发）仅管理员可执�?- GitHub Token 等敏感配置安全存�?
---

## 2. 权限设计原则

1. **统一认证**：复用现�?JWT Bearer Token 认证体系，`/api/v1/open-source/*` 端点与学术端点共享同一鉴权中间件�?2. **后端最终边�?*：所有权限判断在后端完成，前端仅�?UI 适配性隐藏�?3. **数据表隔�?*：开源人才的所有业务表�?`os_` 前缀命名，物理隔离避免跨域数据污染�?4. **最小权�?*：普通用户仅能搜�?查看/收藏开源开发者；管理员才能配置仓库绑定和触发采集�?5. **审计留痕**：所有管理员操作（增删改仓库绑定、触�?取消采集任务）写入审计日志�?
---

## 3. 角色权限矩阵

### 3.1 角色定义（复用学术子系统�?
| 角色 | 编码 | 说明 |
|------|------|------|
| 超级管理�?| `super_admin` | 系统最高权�?|
| 管理�?| `admin` | 业务管理�?/ HR 管理�?|
| 普通用�?| `user` | 业务部门用户或平台用�?|

### 3.2 开源子系统权限矩阵

| 功能 | 超级管理�?| 管理�?| 普通用�?|
|------|-----------|--------|----------|
| **浏览** | | | |
| 开源概览页 | �?| �?| �?|
| 开源搜索页 | �?| �?| �?|
| 开发者详情页 | �?| �?| �?|
| **收藏与运�?* | | | |
| 收藏开源开发�?| �?| �?| �?|
| 取消收藏 | �?| �?| ✅（仅自己的�?|
| 创建人才�?| �?| �?| �?|
| 将开发者加入人才池 | �?| �?| �?|
| **配置与管�?* | | | |
| 查看仓库绑定列表 | �?| �?| �?|
| 添加仓库绑定 | �?| �?| �?|
| 编辑仓库绑定 | �?| �?| �?|
| 删除仓库绑定 | �?| �?| �?|
| 查看采集任务 | �?| �?| �?|
| 触发采集任务 | �?| �?| �?|
| 取消采集任务 | �?| �?| �?|
| **系统配置** | | | |
| 配置 GitHub Token | �?| �?| �?|
| 配置代理 | �?| �?| �?|

> 注：管理员只能操作自己创建或权限范围内的资源；超级管理员可操作全部资源�?
---

## 4. 数据隔离策略

### 4.1 表级隔离

| 子系�?| 表前缀 | 示例 |
|--------|--------|------|
| 学术人才 | `core_` / `raw_` / `std_` | `core_talent`, `raw_author` |
| 开源人�?| `os_` | `os_developer`, `os_repository` |
| 共享 IAM | `iam_` | `iam_user_account` |

**隔离收益**�?- 可以独立备份/恢复开源人才数�?- 可以独立清空/重建开源人才数据（`TRUNCATE os_*` 不影响学术数据）
- 避免命名冲突和误操作

### 4.2 收藏与人才池隔离

| 子系�?| 收藏�?| 人才池表 | 池成员表 |
|--------|--------|----------|----------|
| 学术 | `favourite` | `talent_pool` | `pool_member` |
| 开�?| `os_favourite` | `os_talent_pool` | `os_pool_member` |

**前端适配**�?- `/favorites` 页面需同时查询学术收藏和开源收藏，通过 `domain` 标签区分展示
- 人才池同理，支持创建"混合�?（未来扩展）或保持子系统独立�?
### 4.3 数据可见性规�?
开源子系统当前版本**不引�?*学术子系统的三维权限（学�?国家/技术要素），所有登录用户均可查看全部开源开发者数据。原因：
- GitHub 数据为公开数据，无需按学�?国家过滤
- 技术要素筛选由用户在前端自主选择，不做后端权限裁�?
> 未来若需按技术要素做范围权限，可�?`iam_user_scope` 中扩�?`scope_type = 'opensource_tech_element'`�?
---

## 5. API 层权限控�?
### 5.1 端点权限分类

```python
# endpoints.py 中的权限装饰器使用示�?
from app.core.auth import require_auth, require_admin

router = APIRouter(prefix="/open-source")

# 公开读取（登录用户即可）
@router.get("/stats")
@require_auth
async def get_stats(...):
    ...

@router.get("/developers")
@require_auth
async def list_developers(...):
    ...

@router.get("/developers/{developer_id}")
@require_auth
async def get_developer(...):
    ...

# 收藏操作（登录用户，后端校验只能操作自己的收藏）
@router.post("/favourites")
@require_auth
async def add_favorite(..., current_user: User = Depends(get_current_user)):
    # 自动绑定 current_user.user_id
    ...

# 管理员操�?@router.get("/repo-configs")
@require_admin
async def list_repo_configs(...):
    ...

@router.post("/repo-configs")
@require_admin
async def create_repo_config(...):
    ...

@router.post("/collect/tasks")
@require_admin
async def create_collect_task(...):
    ...
```

### 5.2 收藏 ownership 校验

```python
# 更新/删除收藏时校验所有权
async def update_favorite(
    developer_id: int,
    data: OSFavoriteUpdate,
    current_user: User = Depends(get_current_user),
    session: AsyncSession = Depends(get_async_session),
) -> OSFavoriteResponse:
    favorite = await favorite_repo.get_by_user_and_developer(
        session, current_user.user_id, developer_id
    )
    if not favorite:
        raise HTTPException(status_code=404, detail="Favorite not found")

    # 仅超级管理员可修改他人收�?    if favorite.user_id != current_user.user_id and current_user.role != "super_admin":
        raise HTTPException(status_code=403, detail="Cannot modify other's favorite")

    return await favorite_repo.update(session, favorite, data)
```

---

## 6. GitHub Token 安全设计

### 6.1 存储方案

GitHub Token 支持两种配置方式�?
**方式一：环境变量（推荐用于部署�?*
```bash
GITHUB_TOKENS="ghp_xxx,ghp_yyy,ghp_zzz"
```
- 存储于服务器环境变量，不进入数据�?- 重启服务生效
- 适合生产环境固定 Token 场景

**方式二：系统配置页（推荐用于动态调整）**
- Token 存储�?`system_config` 表（或专门的 `os_github_token` 表）
- **必须加密存储**：使�?AES-256-GCM 加密，密钥来�?`SECRET_KEY`
- 前端展示时脱敏：`ghp_xxxxxxxxxxxxABCD`（仅展示�?4 位）

### 6.2 加密实现示例

```python
from cryptography.fernet import Fernet
import hashlib
import base64

def _derive_key(secret_key: str) -> bytes:
    """�?SECRET_KEY 派生加密密钥"""
    key = hashlib.sha256(secret_key.encode()).digest()
    return base64.urlsafe_b64encode(key)

class TokenVault:
    def __init__(self, secret_key: str):
        self._fernet = Fernet(_derive_key(secret_key))

    def encrypt(self, token: str) -> str:
        return self._fernet.encrypt(token.encode()).decode()

    def decrypt(self, encrypted: str) -> str:
        return self._fernet.decrypt(encrypted.encode()).decode()
```

### 6.3 Token 使用审计

- 每次 Token 轮换时记录日志（哪个 Token 被使用、剩余配额）
- Token 配置变更（新�?删除/更新）写入审计日�?- Token 耗尽时发送告警（日志级别 warning�?
---

## 7. 代理安全

### 7.1 代理配置

内网环境可能需要代理访�?GitHub API�?- 代理配置复用现有 `system_config` 的代理配置（`PROXY_URL`�?- `GitHubClient` 初始化时读取代理配置并透传�?`aiohttp.ClientSession`

### 7.2 代理认证

若代理需要认证：
- 用户名密码存储方式与 GitHub Token 相同（加密存储）
- 密码在前端展示时脱敏

---

## 8. 审计日志

### 8.1 审计事件清单（开源子系统特有�?
| 事件类型 | 触发场景 | 记录内容 |
|----------|----------|----------|
| `os_repo_config_create` | 添加仓库绑定 | repo_full_name, tech_element, operator |
| `os_repo_config_update` | 编辑仓库绑定 | repo_config_id, changed_fields, operator |
| `os_repo_config_delete` | 删除仓库绑定 | repo_config_id, repo_full_name, operator |
| `os_collect_task_create` | 触发采集任务 | task_id, config_json, operator |
| `os_collect_task_cancel` | 取消采集任务 | task_id, operator |
| `os_github_token_update` | 更新 GitHub Token | token_count, operator |
| `os_embedding_generate` | 批量生成嵌入 | batch_size, operator |

### 8.2 审计实现

复用现有审计日志体系（`audit_log` 表或类似机制），通过装饰器自动记录：

```python
@router.post("/repo-configs")
@require_admin
@audit_log(event_type="os_repo_config_create", resource_type="repo_config")
async def create_repo_config(...):
    ...
```

---

## 9. 安全测试要求

| 测试�?| 覆盖场景 |
|--------|----------|
| 未认证访�?| 不带 Token 访问 `/open-source/developers` �?401 |
| 普通用户访问管理接�?| 用户访问 `POST /repo-configs` �?403 |
| 修改他人收藏 | 用户 A 修改用户 B 的收�?�?403 |
| Token 加密验证 | 数据库中�?Token 为密文，解密后与原始值一�?|
| SQL 注入防护 | �?`repo_full_name` 中注�?`' OR '1'='1` �?被参数化查询拦截 |
| XSS 防护 | 开发�?bio 中包�?`<script>` �?前端转义展示 |

---

## 10. 文档结论

本文档定义了开源人才子系统的权限与安全体系�?- **角色矩阵**�? 类角�?× 5 大功能板块，明确每个操作的准入角�?- **数据隔离**：`os_` 前缀表物理隔离，收藏/人才池独立表存储
- **API 权限**：`@require_auth` �?`@require_admin` 分层控制
- **Token 安全**：AES-256-GCM 加密 + 前端脱敏 + 审计留痕
- **代理安全**：复用现有代理配置，认证信息加密存储
- **审计日志**�? 类开源特有事件自动记�?
实现时必须确保后端权限校验为最终边界，前端隐藏仅为体验优化�?