# 开源人才子系统 - 仓库配置管理 (v2.0.0)

**文档编号**�?2  
**版本**：v2.0.0  
**状�?*：后端已实现，前�?UI 缺失  
**适用范围**：智能人才库—开源人才子系统 V2.0.0

---

## 1. 功能定位

仓库配置管理是开源人才子系统�?*数据源入�?*。管理员通过该功能将 GitHub 仓库与技术要素（ai/robotics/data_science/networks/systems/security）绑定，采集器据此确定从哪些仓库开始爬取贡献者数据�?
**核心能力**�?- 绑定/解绑仓库 �?技术领�?- 启用/停用采集开�?- 按技术领域批量筛选仓�?- 预置 35 个标杆仓库，开箱即�?
---

## 2. 数据模型

### 2.1 核心表：`os_repo_config`

| 字段 | 类型 | 说明 |
|------|------|------|
| `repo_config_id` | SERIAL PK | 自增主键 |
| `repo_full_name` | VARCHAR(255), UNIQUE | GitHub 仓库全名，如 `pytorch/pytorch` |
| `display_name` | VARCHAR(255) | 展示名称 |
| `description` | TEXT | 仓库描述 |
| `tech_element` | VARCHAR(50) | 技术要素代码（ai/robotics/.../security�?|
| `tech_direction_id` | INT, FK �?`core_tech_direction` | 可选：精确到技术方�?|
| `language` | VARCHAR(50) | 主要编程语言 |
| `stars_count` | INT, DEFAULT 0 | 仓库 Star 数（缓存�?|
| `is_active` | BOOLEAN, DEFAULT true | 是否在前端展�?|
| `collect_enabled` | BOOLEAN, DEFAULT true | 是否参与采集 |
| `notes` | TEXT | 管理员备�?|
| `created_by` | INT, FK �?`iam_user_account` | 创建�?|
| `created_at` / `updated_at` | TIMESTAMP | 自动维护 |

### 2.2 关联�?
- `core_tech_direction`：技术方向字典表（学术子系统复用�?- `os_repo_mapping`：仓�?�?技术方向的权重映射（用于精细化采集�?
---

## 3. 后端 API

### 3.1 接口清单

| 方法 | 端点 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/v1/open-source/repo-configs` | Admin | 分页列表，支持按 tech_element/is_active/collect_enabled 筛�?|
| POST | `/api/v1/open-source/repo-configs` | Admin | 新增仓库绑定 |
| GET | `/api/v1/open-source/repo-configs/{id}` | Admin | 单条详情 |
| PUT | `/api/v1/open-source/repo-configs/{id}` | Admin | 更新绑定信息 |
| DELETE | `/api/v1/open-source/repo-configs/{id}` | Admin | 删除绑定 |

### 3.2 请求/响应示例

**创建仓库绑定**�?
```bash
POST /api/v1/open-source/repo-configs
Authorization: Bearer <admin-jwt>
Content-Type: application/json

{
  "repo_full_name": "microsoft/DeepSpeed",
  "display_name": "DeepSpeed",
  "description": "Deep learning optimization library",
  "tech_element": "ai",
  "tech_direction_id": 1,
  "language": "Python"
}
```

**响应�?01�?*�?
```json
{
  "repo_config_id": 36,
  "repo_full_name": "microsoft/DeepSpeed",
  "display_name": "DeepSpeed",
  "tech_element": "ai",
  "tech_direction_id": 1,
  "language": "Python",
  "stars_count": 0,
  "is_active": true,
  "collect_enabled": true,
  "created_at": "2026-04-30T12:00:00Z"
}
```

### 3.3 业务规则

1. **repo_full_name 唯一�?*：`owner/repo` 格式，正则校�?`^[\w.-]+/[\w.-]+$`
2. **tech_element 白名�?*：仅允许 `ai`, `robotics`, `data_science`, `networks`, `systems`, `security`
3. **软删�?vs 硬删�?*：当前为硬删除；未来可改�?`is_active=false` 软删�?4. **创建人自动填�?*：后端从 JWT token 中提�?`user_id` 写入 `created_by`

---

## 4. 预置仓库清单

运行 `python scripts/seed_os_repo_configs.py` 后，数据库中自动插入 35 个标杆仓库：

| 技术领�?| 仓库数量 | 示例仓库 |
|----------|---------|---------|
| ai | 6 | pytorch/pytorch, tensorflow/tensorflow, huggingface/transformers, scikit-learn/scikit-learn, microsoft/DeepSpeed, apache/spark |
| robotics | 5 | ros/ros, ros2/ros2, ArduPilot/ardupilot, NVIDIA-Omniverse/IsaacSim, google-research/google-research |
| data_science | 6 | pandas-dev/pandas, numpy/numpy, jupyter/jupyter, matplotlib/matplotlib, apache/arrow, dask/dask |
| networks | 6 | torvalds/linux, envoyproxy/envoy, grpc/grpc, openvswitch/ovs, cloudflare/cloudflared, FRRouting/frr |
| systems | 6 | golang/go, rust-lang/rust, kubernetes/kubernetes, moby/moby, redis/redis, apache/kafka |
| security | 6 | zaproxy/zaproxy, rapid7/metasploit-framework, sqlmapproject/sqlmap, nmap/nmap, mitmproxy/mitmproxy, wireshark/wireshark |

**种子脚本特�?*�?- 幂等执行：按 `repo_full_name` 去重，已存在则跳�?- 不依赖外键：`tech_direction_id` �?NULL（简化版）

---

## 5. 前端设计（待实现�?
### 5.1 页面位置

嵌入系统配置�?`/system-config`，新�?**"开源仓库配�?** �?Tab�?
```
系统配置
├── 采集配置
�?  ├── 技术领域配�?   �?学术子系统现�?�?  ├── 采集任务        �?学术子系统现�?�?  └── 开源仓库配�?   �?【新增�?├── LLM 配置           �?现有
└── 代理配置           �?现有
```

### 5.2 页面结构

```
┌─────────────────────────────────────────�?�?[新增仓库]  [批量启用]  [批量停用]         �?├─────────────────────────────────────────�?�?筛�? [技术领�?▼] [状�?▼] [搜索...]    �?├─────────────────────────────────────────�?�?仓库列表 (Table)                         �?�?├─ 仓库�?     �?技术领�?�?语言 �?Stars �?�?├─ pytorch/... �?AI       �?Python �?85k �?�?├─ ros/ros     �?机器�?  �?C++  �?12k �?�?└─ ...         �?...      �?...  �?... �?├─────────────────────────────────────────�?�?分页: < 1 2 3 ... 10 >                  �?└─────────────────────────────────────────�?```

### 5.3 新增/编辑弹窗

```
┌─────────────────────────────�?�?仓库绑定配置                   �?├─────────────────────────────�?�?GitHub 仓库全名 *             �?�?[ owner/repo                ] �?�?显示名称                      �?�?[ DeepSpeed                 ] �?�?描述                          �?�?[ ...                       ] �?�?技术要�?*                    �?�?[ �?人工智能 (ai)           ] �?�?技术方�?                     �?�?[ �?深度学习                 ] �?�?主要语言                      �?�?[ Python                    ] �?�?[✓] 启用展示                  �?�?[✓] 参与采集                  �?�?备注                          �?�?[ ...                       ] �?├─────────────────────────────�?�?[取消]        [保存]         �?└─────────────────────────────�?```

### 5.4 前端组件清单

| 组件 | 说明 | 复用/新建 |
|------|------|----------|
| `OSRepoConfigTab` | 系统配置页内的子 Tab 容器 | 新建 |
| `RepoConfigTable` | 仓库列表表格 | 新建 |
| `RepoConfigForm` | 新增/编辑弹窗表单 | 新建 |
| `TechElementSelect` | 技术要素下拉选择 | 可复用学术子系统�?`TechDomainSelect` |

### 5.5 API 调用

```typescript
// api.ts 已定义（无需新增�?api.openSource.repoConfigs.list(params)
api.openSource.repoConfigs.create(data)
api.openSource.repoConfigs.update(id, data)
api.openSource.repoConfigs.delete(id)
```

---

## 6. 与采集流水线的衔�?
```
┌──────────────�?    ┌──────────────────�?    ┌──────────────�?�?os_repo_config│────→│ Collector        │────→│ os_developer �?�?(仓库绑定�?   �?    �?(8阶段采集流水�? �?    �?(开发者表)    �?└──────────────�?    └──────────────────�?    └──────────────�?       �?                     �?       �?筛选条�?             �?Phase 1: 遍历 collect_enabled=true
       �?is_active=true        �?       的仓库，调用 GitHub API
       �?collect_enabled=true  �?       获取贡献者列�?       └──────────────────────�?```

**采集触发方式**�?1. 手动触发：系统配置页 �?开源仓库配�?�?选择仓库 �?点击"开始采�?
2. 定时触发（未来）：Cron 任务每晚扫描 `collect_enabled=true` 的仓库增量更�?
---

## 7. 实现状�?
| 模块 | 状�?| 备注 |
|------|------|------|
| 数据库表 + 索引 | �?已完�?| 迁移 `046_add_open_source_tables.py` |
| 后端 CRUD API | �?已完�?| `open_source.py` �?98-217 �?|
| 种子脚本 | �?已完�?| `scripts/seed_os_repo_configs.py` |
| 前端系统配置 Tab | �?缺失 | 未在 `SystemConfigPage.tsx` 中注�?|
| 前端仓库列表组件 | �?缺失 | 未实�?`OSRepoConfigTab` |
| 前端新增/编辑弹窗 | �?缺失 | 未实�?`RepoConfigForm` |

---

## 8. 下一步工�?
如需补全前端，按以下顺序实现�?
1. **新建 `frontend/src/pages/system/OSRepoConfigTab.tsx`**
   - 使用 Ant Design `Table` + `Modal` + `Form`
   - 调用 `api.openSource.repoConfigs.*` 系列方法
   - 集成�?`SystemConfigPage.tsx` �?Tabs �?
2. **扩展 `SystemConfigPage.tsx`**
   - �?`collectSubTab` 中新�?`'opensource-repos'` key
   - �?`loadData` 逻辑中增加对应加载函�?
3. **权限控制**
   - 复用现有 `isAdmin` 判断，仅管理员可见该 Tab
   - API 层已�?`require_admin` 校验，无需额外改动
