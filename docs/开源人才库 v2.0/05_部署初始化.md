# 开源人才子系统 - 部署与初始化 (v2.0.0)

## 1. 环境依赖

### 后端
- Python 3.11+
- PostgreSQL 14+（需 pgvector 扩展�?- Redis（可选，用于缓存降级�?
### 前端
- Node.js 20+
- npm 10+

## 2. 数据库迁�?
```bash
cd backend

# 1. 激活虚拟环�?.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/Mac

# 2. 安装依赖
pip install -r requirements.txt

# 3. 运行迁移�?46-051�?alembic upgrade head
```

**迁移清单**:

| 迁移 | 说明 |
|------|------|
| 046 | 创建 12 张核心表（developer/repo/contribution/skill/org/dev_org/embedding/mapping/raw�?|
| 047 | 创建 3 �?IAM 表（favourite/talent_pool/pool_member�?|
| 048 | 添加性能索引（含 GIN 索引�?|
| 049 | 创建 os_collect_task 任务�?|
| 050 | �?github_repo_id 索引 |
| **051** | **创建 os_repo_config 配置层表** |

## 3. 预置数据初始�?
### 3.1 预置仓库绑定

```bash
cd backend
python scripts/seed_os_repo_configs.py
```

**输出示例**:
```
Seeded 35 OS repo configs, skipped 0 duplicates.

By tech element:
  ai: 6 repos
  robotics: 5 repos
  data_science: 6 repos
  networks: 6 repos
  systems: 6 repos
  security: 6 repos
```

### 3.2 技术领域初始化（如未执行）

```bash
cd backend
python scripts/seed_tech_domains.py
```

### 3.3 默认管理员账�?
```bash
cd backend
python scripts/create_admin.py
```

默认: `admin` / `admin123`

## 4. GitHub Token 配置

### 方式一: 环境变量
```bash
export GITHUB_TOKENS="ghp_xxx,ghp_yyy,ghp_zzz"
```

### 方式�? 系统配置页（推荐�?1. 登录系统 �?系统配置 �?选择 "其他配置"
2. 找到 "GitHub API Token 配置"
3. 输入逗号分隔�?Token 列表
4. 保存

**获取 Token**:
- GitHub �?Settings �?Developer settings �?Personal access tokens �?Tokens (classic)
- 权限: 无需特殊权限，public 数据即可

## 5. 启动服务

### 后端
```bash
cd backend
uvicorn app.main:app --reload --port 8003
```

### 前端
```bash
cd frontend
npm install
npm run dev  # port 2012
```

## 6. 首次采集

1. 打开系统配置 �?采集配置 �?**仓库绑定**
   - 确认 35 个预置仓库已加载
   - 可添�?编辑/删除仓库绑定

2. 切换�?**开源采�?* 标签�?   - 选择 "从绑定仓库采�?
   - 选择要采集的技术领域（�?ai, systems�?   - 点击 "开始采�?

3. 观察任务状�?   - 任务 ID、进度、当前步骤实时更�?   - �?5-10 分钟完成（取决于仓库数和贡献者数�?
## 7. 环境变量清单

| 变量 | 必填 | 说明 |
|------|------|------|
| `DATABASE_URL` | �?| PostgreSQL 连接�?|
| `GITHUB_TOKENS` | �?| 逗号分隔�?GitHub Token |
| `LLM_API_KEY` | �?| LLM API Key（用于嵌入和 JD 匹配�?|
| `LLM_API_BASE` | �?| LLM API 基础 URL |
| `LLM_MODEL` | �?| 对话模型�?|
| `LLM_EMBEDDING_MODEL` | �?| 嵌入模型�?|
| `PROXY_URL` | �?| 代理 URL（内网环境） |
| `REDIS_URL` | �?| Redis 连接串（可选） |

## 8. 验证检查清�?
- [ ] `alembic upgrade head` 执行成功
- [ ] `seed_os_repo_configs.py` 输出 35 条记�?- [ ] `GET /api/v1/open-source/repo-configs` 返回 35 条数�?- [ ] `GET /api/v1/system-config/github` 可读�?写入 Token
- [ ] 采集任务可正常启动并完成
- [ ] `GET /api/v1/open-source/stats` 返回非零统计
- [ ] 前端搜索页可正常显示开发�?
## 9. 故障排查

### 问题: `UndefinedTableError: os_repo_config`
**原因**: 迁移未执�?**解决**: `alembic upgrade head`

### 问题: `NoReferencedTableError: core_tech_direction`
**原因**: seed 脚本未导�?TechDirection 模型
**解决**: seed 脚本已修复，重新运行 `python scripts/seed_os_repo_configs.py`

### 问题: 采集任务立即失败，error_message �?"No GitHub tokens configured"
**原因**: 未配�?GitHub Token
**解决**: 在系统配置页配置，或设置环境变量 `GITHUB_TOKENS`

### 问题: 前端仓库绑定页显示为�?**原因**: `OSRepoConfigResponse` 缺少 `ConfigDict(from_attributes=True)`
**解决**: 已修复，重启后端服务

### 问题: 分页显示 "�?1 �? 但实际有多条
**原因**: `repo_config_repository.list()` count 查询错误
**解决**: 已修复为 `func.count()`，重启后端服�?