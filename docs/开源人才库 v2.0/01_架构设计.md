# 开源人才子系统 - 架构设计 (v2.0.0)

## 1. 系统架构�?
```
┌─────────────────────────────────────────────────────────────────�?�?                        前端 (React 18 + Vite)                   �?�? ┌─────────────�? ┌─────────────�? ┌─────────────────────────�? �?�? �?概览�?     �? �?搜索�?     �? �?系统配置 �?仓库绑定/采集 �? �?�? �?OpenSource  �? �?OpenSource  �? �?                        �? �?�? �?  Page      �? �?SearchPage  �? �? OSRepoConfigPage       �? �?�? └─────────────�? └─────────────�? �? OSCollectPanel         �? �?�? ┌─────────────�? ┌─────────────�? └─────────────────────────�? �?�? �?详情�?     �? �?收藏�?     �?                               �?�? │Developer    �? �?Favorites   �?                               �?�? �? Detail     �? �?(第三标签)  �?                               �?�? └─────────────�? └─────────────�?                               �?└──────────────────────────┬──────────────────────────────────────�?                           �?HTTP /api/v1/open-source/*
┌──────────────────────────▼──────────────────────────────────────�?�?                     后端 (FastAPI + SQLAlchemy async)           �?�?                                                                 �?�? ┌──────────────────────────────────────────────────────────�?  �?�? �? API �?(endpoints.py)                                    �?  �?�? �? ├── /repo-configs    CRUD 仓库绑定                      �?  �?�? �? ├── /developers      列表/详情/对比/相似推荐            �?  �?�? �? ├── /search          关键�?语义/混合搜索               �?  �?�? �? ├── /favourites      收藏 CRUD                          �?  �?�? �? ├── /talent-pools    人才�?CRUD                        �?  �?�? �? ├── /collect/tasks   采集任务管理                       �?  �?�? �? ├── /jd-match        JD 岗位匹配                        �?  �?�? �? ├── /stats           统计面板                           �?  �?�? �? └── /embeddings/*    向量嵌入管理                       �?  �?�? └──────────────────────────────────────────────────────────�?  �?�?                             �?                                  �?�? ┌───────────────────────────▼─────────────────────────────�?   �?�? �? Service �?                                              �?   �?�? �? ├── OSDeveloperService    画像/对比/相似               �?   �?�? �? ├── OSSearchService       搜索门面 (keyword/semantic)  �?   �?�? �? ├── OSRepoConfigService   仓库配置 CRUD + 校验         �?   �?�? �? ├── OSJDMatchService      JD 解析 + 匹配评分           �?   �?�? �? ├── OSStatsService        统计聚合                     �?   �?�? �? └── OSEmbeddingService    向量生成 + 搜索              �?   �?�? └──────────────────────────────────────────────────────────�?  �?�?                             �?                                  �?�? ┌───────────────────────────▼─────────────────────────────�?   �?�? �? Repository �?                                           �?   �?�? �? ├── OSDeveloperRepository                              �?   �?�? �? ├── OSEmbeddingRepository    pgvector/SQLite 兼容       �?   �?�? �? └── OSRepoConfigRepository                               �?   �?�? └──────────────────────────────────────────────────────────�?  �?�?                             �?                                  �?�? ┌───────────────────────────▼─────────────────────────────�?   �?�? �? Collector 流水�?(8 Phase)                              �?   �?�? �? ├── Phase 1: fetch_repos      仓库发现                 �?   �?�? �? ├── Phase 2: fetch_developers 开发者采�?              �?   �?�? �? ├── Phase 3: fetch_contributions 贡献数据              �?   �?�? �? ├── Phase 4: aggregate_languages 语言聚合              �?   �?�? �? ├── Phase 5: calc_tech_belong 技术归�?                �?   �?�? �? ├── Phase 6: sync_serving     同步服务�?              �?   �?�? �? ├── Phase 7: generate_embeddings 向量嵌入              �?   �?�? �? └── Phase 8: build_stats      统计构建                 �?   �?�? └──────────────────────────────────────────────────────────�?  �?�?                                                                 �?�? ┌──────────────────────────────────────────────────────────�?  �?�? �? GitHubClient (REST + GraphQL + Token 轮换 + 代理)       �?  �?�? └──────────────────────────────────────────────────────────�?  �?�?                                                                 �?└──────────────────────────────────────────────────────────────────�?                           �?┌──────────────────────────▼──────────────────────────────────────�?�?                        PostgreSQL + pgvector                    �?�? os_developer | os_repository | os_contribution | os_embedding   �?�? os_repo_config | os_collect_task | os_favourite | os_talent_pool�?└──────────────────────────────────────────────────────────────────�?```

## 2. 与学术子系统的对�?
| 层面 | 学术子系�?| 开源子系统 |
|------|-----------|-----------|
| **配置�?* | `config_venue` + `venue_tech_binding` | `os_repo_config`（仓库→技术领域绑定） |
| **采集入口** | Venue（顶会顶刊） | Repository（开源仓库） |
| **原始数据** | `raw_work`, `raw_author`, `raw_institution` | `os_raw_developer` |
| **标准化层** | `std_author`, `std_school` | 无（GitHub 数据相对干净�?|
| **服务�?* | `core_talent`, `core_school` | `os_developer`, `os_repository` |
| **身份标识** | OpenAlex Author ID | GitHub Login |
| **能力指标** | H-index, 引用�?| Stars, Commits, PRs, Issues |

## 3. 核心设计决策

### 3.1 仓库绑定驱动采集（Config-Layer First�?
传统方式直接输入 GitHub orgs，但微软/腾讯等组织的公开成员数为 0，无法直接获取�?
**解决方案**: 管理员预先绑定知名仓库到技术领域，采集时：
1. �?`os_repo_config` 读取 `collect_enabled=True` 的仓�?2. 调用 `/repos/{owner}/{repo}/contributors` 获取贡献�?3. 回查贡献�?Profile，汇总开发者列�?
```python
# Phase 1: fetch_repos.py 双模�?if config.tech_elements:
    # 模式1: 从配置层读取绑定仓库
    repos = await _fetch_from_repo_config(tech_elements=config.tech_elements)
else:
    # 模式2: fallback 到手�?orgs/topics 搜索
    repos = await _fetch_from_search(config)
```

### 3.2 Token 轮换与速率限制

GitHub API 速率限制：未认证 60/h，认�?5000/h�?
**解决方案**:
- 支持�?Token 配置（`GITHUB_TOKENS=token1,token2`�?- 系统配置页支持动态增�?Token
- `GitHubClient` 内部自动轮换，单 Token 耗尽时切�?- 遇到 403/429 时指数退避重�?
### 3.3 向量嵌入与语义搜�?
- 使用 LLM Gateway 生成开发�?profile 的向量嵌�?- `os_embedding` 表存�?`developer_id` + `vector_type` + `embedding`
- pgvector `<=>` 操作符做余弦相似度搜�?- 支持混合搜索（RRF 融合关键�?+ 语义�?
### 3.4 表隔离策�?
所有开源人才表�?`os_` 前缀命名，与学术人才表完全隔离：
- 可以独立备份/恢复
- 可以独立清空/重建
- 避免命名冲突

## 4. 技术选型

| 组件 | 选型 |
|------|------|
| GitHub API 客户�?| 自定�?`GitHubClient` (aiohttp + token 轮换) |
| 向量数据�?| pgvector (PostgreSQL extension) |
| 嵌入生成 | LLM Gateway (�?JD 匹配共用) |
| 任务执行 | asyncio.create_task() 后台任务 |
| 任务持久�?| `os_collect_task` �?|
| 缓存 | 无专用缓存（依赖 PostgreSQL 索引 + React Query�?|

## 5. 扩展�?
| 扩展 | 说明 |
|------|------|
| 新增技术领�?| 修改 `VALID_TECH_ELEMENTS` + `seed_os_repo_configs.py` |
| 新增数据�?| GitLab/Gitee 等需新增 `GitLabClient` + 对应 Phase |
| 实时通知 | 接入 WebSocket 推送采集进�?|
| 定时采集 | 接入 Celery/APScheduler 做定时增量采�?|
| 社交图谱 | `OSDeveloperOrg` + `OSContribution` 可构建协作网�?|
