# 开源人才子系统 - 测试策略与用�?(v2.0.0)

**文档编号**�?9  
**版本**：v2.0.0  
**状�?*：设计稿  
**适用范围**：智能人才库—开源人才子系统 V2.0.0

---

## 1. 测试目标

确保开源人才子系统在功能正确性、接口稳定性、采集可靠性、权限安全性和前端用户体验方面达到可上线标准�?
---

## 2. 测试范围

```
┌─────────────────────────────────────────────�?�?后端测试                                     �?�? ├── 单元测试（pytest�?                      �?�? �?   ├── GitHubClient（mock + 重试�?        �?�? �?   ├── SearchService�?种模式）             �?�? �?   ├── EmbeddingService（去�?+ 生成�?     �?�? �?   ├── DeveloperService（对�?+ 推荐�?     �?�? �?   ├── RepoConfigService（校验）            �?�? �?   └── Phase 处理器（8阶段�?               �?�? ├── 集成测试（pytest + test db�?            �?�? �?   ├── API 端点（CRUD + 搜索 + 收藏�?      �?�? �?   ├── 采集流水线（端到端）                 �?�? �?   └── 权限控制（角色矩阵验证）              �?�? └── 安全测试                                �?�?      ├── SQL 注入防护                        �?�?      └── 越权访问防护                        �?├─────────────────────────────────────────────�?�?前端测试                                     �?�? ├── 单元测试（Vitest + jsdom�?              �?�? �?   ├── 组件渲染（DeveloperCard 等）         �?�? �?   └── Hooks 逻辑（筛选、分页）             �?�? └── E2E 测试（Playwright�?                  �?�?      ├── 页面跳转 + 搜索流程                 �?�?      └── 收藏 + 人才池操�?                  �?└─────────────────────────────────────────────�?```

---

## 3. 后端测试

### 3.1 测试环境

- **测试数据�?*：独立的 PostgreSQL 测试库（`talent_db_test`），每个测试函数结束�?`DROP ALL TABLES`
- **GitHub API**：全部使�?`aioresponses` �?`responses` mock，禁止真实调�?- **LLM Gateway**：使�?`MagicMock` 模拟嵌入生成，返回固定维度向�?
### 3.2 单元测试

#### GitHubClient 测试

```python
# tests/modules/open_source/test_github_client.py
import pytest
from aioresponses import aioresponses

class TestGitHubClient:
    @pytest.fixture
    def client(self):
        return GitHubClient(tokens=["ghp_test_token"])

    @pytest.mark.asyncio
    async def test_get_repo_success(self, client):
        with aioresponses() as m:
            m.get(
                "https://api.github.com/repos/pytorch/pytorch",
                payload={"id": 65600975, "full_name": "pytorch/pytorch"},
                headers={"X-RateLimit-Remaining": "4999"},
            )
            repo = await client.get_repo("pytorch", "pytorch")
            assert repo["full_name"] == "pytorch/pytorch"

    @pytest.mark.asyncio
    async def test_rate_limit_retry(self, client):
        """测试 403 rate limit 触发退避重�?""
        with aioresponses() as m:
            m.get(
                "https://api.github.com/repos/pytorch/pytorch",
                status=403,
                body="API rate limit exceeded",
            )
            m.get(
                "https://api.github.com/repos/pytorch/pytorch",
                payload={"id": 65600975, "full_name": "pytorch/pytorch"},
                headers={"X-RateLimit-Remaining": "4998"},
            )
            repo = await client.get_repo("pytorch", "pytorch")
            assert repo["full_name"] == "pytorch/pytorch"

    @pytest.mark.asyncio
    async def test_404_not_found(self, client):
        """测试 404 不重试，直接抛异�?""
        with aioresponses() as m:
            m.get("https://api.github.com/repos/xxx/yyy", status=404)
            with pytest.raises(GitHubNotFoundError):
                await client.get_repo("xxx", "yyy")

    @pytest.mark.asyncio
    async def test_token_rotation(self, client):
        """测试�?Token 轮换"""
        client = GitHubClient(tokens=["token_a", "token_b"])
        with aioresponses() as m:
            m.get(
                "https://api.github.com/repos/a/a",
                payload={"id": 1},
                headers={"X-RateLimit-Remaining": "0"},
            )
            m.get(
                "https://api.github.com/repos/b/b",
                payload={"id": 2},
                headers={"X-RateLimit-Remaining": "5000"},
            )
            await client.get_repo("a", "a")
            repo = await client.get_repo("b", "b")
            assert repo["id"] == 2
```

#### SearchService 测试

```python
# tests/modules/open_source/test_search_service.py
class TestOSSearchService:
    @pytest.mark.asyncio
    async def test_keyword_search_by_name(self, test_session, sample_developers):
        """关键词搜索匹�?name"""
        service = OSSearchService(...)
        result = await service.search(query="zhang", mode=SearchMode.KEYWORD)
        assert len(result.data) > 0
        assert all("zhang" in (d.name or "").lower() for d in result.data)

    @pytest.mark.asyncio
    async def test_keyword_search_filter_by_tech_element(self, test_session, sample_developers):
        """技术领域筛�?""
        service = OSSearchService(...)
        filters = OSSearchFilters(tech_elements=["ai"])
        result = await service.search(query="", mode=SearchMode.KEYWORD, filters=filters)
        assert all("ai" in d.tech_tags for d in result.data)

    @pytest.mark.asyncio
    async def test_semantic_search(self, test_session, sample_developers_with_embeddings):
        """语义搜索返回向量近邻"""
        service = OSSearchService(...)
        result = await service.search(query="machine learning expert", mode=SearchMode.SEMANTIC)
        assert len(result.data) > 0

    @pytest.mark.asyncio
    async def test_hybrid_search_rrf(self, test_session, sample_developers_with_embeddings):
        """混合搜索 RRF 融合结果�?""
        service = OSSearchService(...)
        result = await service.search(query="python", mode=SearchMode.HYBRID)
        # 验证结果同时包含 keyword �?semantic 的命�?        assert len(result.data) > 0
```

#### RepoConfigService 测试

```python
# tests/modules/open_source/test_repo_config_service.py
class TestOSRepoConfigService:
    @pytest.mark.asyncio
    async def test_create_invalid_repo_full_name(self, test_session):
        """非法格式应抛 ValidationError"""
        service = OSRepoConfigService(...)
        with pytest.raises(ValidationError):
            await service.create(OSRepoConfigCreate(repo_full_name="invalid", tech_element="ai"))

    @pytest.mark.asyncio
    async def test_create_invalid_tech_element(self, test_session):
        """非法技术领域应�?ValidationError"""
        service = OSRepoConfigService(...)
        with pytest.raises(ValidationError):
            await service.create(OSRepoConfigCreate(repo_full_name="a/b", tech_element="invalid"))

    @pytest.mark.asyncio
    async def test_create_duplicate_repo(self, test_session, sample_repo_config):
        """重复仓库应抛 DuplicateError"""
        service = OSRepoConfigService(...)
        with pytest.raises(DuplicateError):
            await service.create(
                OSRepoConfigCreate(repo_full_name=sample_repo_config.repo_full_name, tech_element="ai")
            )
```

### 3.3 集成测试

#### API 端点集成测试

```python
# tests/modules/open_source/test_api_integration.py
class TestOpenSourceAPI:
    @pytest.mark.asyncio
    async def test_list_developers(self, client, sample_developers):
        """GET /open-source/developers"""
        response = await client.get("/api/v1/open-source/developers")
        assert response.status_code == 200
        data = response.json()
        assert len(data["data"]) > 0
        assert "total" in data

    @pytest.mark.asyncio
    async def test_search_developers_keyword(self, client, sample_developers):
        """关键词搜�?""
        response = await client.get("/api/v1/open-source/developers?q=pytorch")
        assert response.status_code == 200

    @pytest.mark.asyncio
    async def test_create_repo_config_as_admin(self, admin_client):
        """管理员创建仓库绑�?""
        response = await admin_client.post(
            "/api/v1/open-source/repo-configs",
            json={"repo_full_name": "test/test", "tech_element": "ai"},
        )
        assert response.status_code == 201

    @pytest.mark.asyncio
    async def test_create_repo_config_as_user(self, client):
        """普通用户创建仓库绑定应 403"""
        response = await client.post(
            "/api/v1/open-source/repo-configs",
            json={"repo_full_name": "test/test", "tech_element": "ai"},
        )
        assert response.status_code == 403

    @pytest.mark.asyncio
    async def test_favorite_crud(self, client, sample_developer, sample_user):
        """收藏增删改查"""
        # 添加收藏
        response = await client.post(
            "/api/v1/open-source/favourites",
            json={"developer_id": sample_developer.developer_id, "notes": "重点"},
        )
        assert response.status_code == 200

        # 查询收藏列表
        response = await client.get("/api/v1/open-source/favourites")
        assert response.status_code == 200
        assert len(response.json()["data"]) == 1

        # 删除收藏
        response = await client.delete(
            f"/api/v1/open-source/favourites/{sample_developer.developer_id}"
        )
        assert response.status_code == 200
```

#### 采集流水线集成测�?
```python
# tests/modules/open_source/test_collection_pipeline.py
class TestCollectionPipeline:
    @pytest.mark.asyncio
    @pytest.mark.slow
    async def test_full_pipeline(self, test_session, mock_github_client, sample_repo_configs):
        """
        端到端采集流水线测试（标�?slow，默认跳过）�?        使用 mock GitHubClient 模拟完整数据流�?        """
        config = OSCollectConfig(
            tech_elements=["ai"],
            contributors_per_repo=2,
        )

        orchestrator = OSCollectOrchestrator(
            github_client=mock_github_client,
            ...
        )

        task = await create_collect_task(test_session, config)
        await orchestrator.run(task.task_id, config)

        # 验证 Phase 6 持久化结�?        developers = await test_session.execute(select(OSDeveloper))
        assert len(developers.scalars().all()) > 0

        # 验证任务完成状�?        updated_task = await get_task(test_session, task.task_id)
        assert updated_task.status == "completed"
        assert updated_task.progress_percent == 100
```

### 3.4 权限安全测试

```python
# tests/modules/open_source/test_security.py
class TestOpenSourceSecurity:
    @pytest.mark.asyncio
    async def test_sql_injection_in_repo_full_name(self, admin_client):
        """repo_full_name SQL 注入防护"""
        response = await admin_client.post(
            "/api/v1/open-source/repo-configs",
            json={"repo_full_name": "' OR '1'='1", "tech_element": "ai"},
        )
        # 应被格式校验拦截，返�?400
        assert response.status_code == 400

    @pytest.mark.asyncio
    async def test_xss_in_bio(self, client, sample_developer):
        """开发�?bio XSS 防护（后端不过滤，由前端处理，此处验证数据完整性）"""
        malicious_bio = "<script>alert('xss')</script>"
        # 模拟开发�?bio 含恶意脚�?        # 后端应原样存储，前端应转义展�?
    @pytest.mark.asyncio
    async def test_unauthorized_access(self, client_no_auth):
        """未认证访问应 401"""
        response = await client_no_auth.get("/api/v1/open-source/developers")
        assert response.status_code == 401
```

---

## 4. 前端测试

### 4.1 单元测试（Vitest�?
#### DeveloperCard 组件测试

```tsx
// src/modules/openSource/components/DeveloperCard.test.tsx
import { render, screen } from '@testing-library/react'
import DeveloperCard from './DeveloperCard'

describe('DeveloperCard', () => {
  const mockDev = {
    developer_id: 1,
    github_login: 'testuser',
    name: 'Test User',
    bio: 'Python developer',
    total_stars_received: 1500,
    primary_languages: ['Python', 'Go'],
    tech_tags: ['ai', 'systems'],
  }

  it('renders developer basic info', () => {
    render(<DeveloperCard developer={mockDev} />)
    expect(screen.getByText('Test User')).toBeInTheDocument()
    expect(screen.getByText('@testuser')).toBeInTheDocument()
    expect(screen.getByText('Python')).toBeInTheDocument()
  })

  it('renders compact mode without bio', () => {
    render(<DeveloperCard developer={mockDev} viewMode="compact" />)
    expect(screen.queryByText('Python developer')).not.toBeInTheDocument()
  })

  it('calls onFavoriteToggle when favorite button clicked', () => {
    const mockToggle = vi.fn()
    render(
      <DeveloperCard
        developer={mockDev}
        showActions
        isFavorite={false}
        onFavoriteToggle={mockToggle}
      />
    )
    screen.getByRole('button', { name: /收藏/i }).click()
    expect(mockToggle).toHaveBeenCalledWith(1, true)
  })
})
```

#### FilterPanel 状态测�?
```tsx
// src/modules/openSource/components/OSFilterPanel.test.tsx
describe('OSFilterPanel', () => {
  it('updates query context on filter change', () => {
    const onChange = vi.fn()
    render(<OSFilterPanel onChange={onChange} />)

    // 选择技术领�?    fireEvent.click(screen.getByText('人工智能'))
    expect(onChange).toHaveBeenCalledWith(
      expect.objectContaining({ tech_elements: ['ai'] })
    )
  })
})
```

### 4.2 E2E 测试（Playwright�?
```ts
// tests/openSource.spec.ts
import { test, expect } from '@playwright/test'

test.describe('开源人才子系统 E2E', () => {
  test.beforeEach(async ({ page }) => {
    // 登录
    await page.goto('/login')
    await page.fill('input[name="username"]', 'admin')
    await page.fill('input[name="password"]', 'admin123')
    await page.click('button[type="submit"]')
    await page.waitForURL('/')
  })

  test('开源概览页加载', async ({ page }) => {
    await page.goto('/opensource')
    await expect(page.getByText('开源生�?)).toBeVisible()
    await expect(page.getByText('收录开发�?)).toBeVisible()
  })

  test('搜索开发者流�?, async ({ page }) => {
    await page.goto('/opensource/search')
    await page.fill('input[placeholder="搜索开发�?.."]', 'python')
    await page.click('button:has-text("搜索")')

    // 等待结果加载
    await page.waitForSelector('.developer-card', { timeout: 10000 })
    const cards = await page.locator('.developer-card').count()
    expect(cards).toBeGreaterThan(0)
  })

  test('开发者详情页跳转', async ({ page }) => {
    await page.goto('/opensource/search')
    await page.waitForSelector('.developer-card')
    await page.click('.developer-card:first-child')

    // 验证详情页加�?    await expect(page.locator('text=仓库列表')).toBeVisible()
    await expect(page.locator('text=贡献记录')).toBeVisible()
  })

  test('收藏开发�?, async ({ page }) => {
    await page.goto('/opensource/search')
    await page.waitForSelector('.developer-card')

    // 点击收藏按钮
    await page.click('.developer-card:first-child .favorite-button')
    await expect(page.getByText('已收�?)).toBeVisible()

    // 验证收藏�?    await page.goto('/favorites')
    await expect(page.getByText('开�?)).toBeVisible()
  })
})
```

---

## 5. 验收标准

### 5.1 功能验收

| 验收�?| 通过标准 |
|--------|----------|
| 仓库绑定 CRUD | 管理员可增删改查 35 个预置仓库，格式校验正确 |
| 采集任务 | �?35 个仓库采集，任务状态流转正确，进度更新及时 |
| 开发者搜�?| 关键�?语义/混合三种模式均返回合理结�?|
| 开发者详�?| Profile、仓库、贡献、语言、相似推�?5 个区块完整展�?|
| 收藏与人才池 | 增删改查正常，与学术收藏不混�?|
| JD 匹配 | 输入 JD 文本返回匹配开发者列表及评分理由 |
| 统计面板 | 概览页统计数字与数据库实际数据一�?|

### 5.2 性能验收

| 验收�?| 通过标准 |
|--------|----------|
| 搜索响应时间 | 关键词搜�?< 500ms（P95�?|
| 语义搜索响应时间 | 语义搜索 < 2s（P95，含向量生成�?|
| 详情页加载时�?| 详情页完整加�?< 1s（P95�?|
| 采集任务耗时 | 35 仓库 × 30 贡献�?< 15 分钟 |
| 前端首屏加载 | 概览页首�?< 2s |

### 5.3 安全验收

| 验收�?| 通过标准 |
|--------|----------|
| 未认证访�?| 所�?`/open-source/*` 端点未带 Token 返回 401 |
| 越权操作 | 普通用户访问管理接口返�?403 |
| Token 加密 | 数据库中 GitHub Token 为密文，不可直接读取 |
| SQL 注入 | 所有用户输入通过参数化查询处�?|

---

## 6. 测试运行命令

```bash
# 后端单元测试（排�?slow�?cd backend
uv run pytest tests/modules/open_source/ -v

# 后端全部测试（含 slow�?uv run pytest tests/modules/open_source/ -v --run-slow

# 前端单元测试
cd frontend
npm run test

# E2E 测试
cd frontend
npx playwright test tests/openSource.spec.ts

# 覆盖率报�?uv run pytest tests/modules/open_source/ --cov=app.modules.open_source --cov-report=html
```

---

## 7. 文档结论

本文档定义了开源人才子系统的完整测试策略：
- **后端**：GitHubClient mock 测试、SearchService 三种模式测试、RepoConfigService 校验测试、API 集成测试、采集流水线端到端测试、安全渗透测�?- **前端**：DeveloperCard / FilterPanel 组件单元测试、搜�?详情/收藏 E2E 测试
- **验收标准**：功�?7 项、性能 5 项、安�?4 �?
开发过程中应遵�?先写测试再写实现"（TDD）或"边写边测"策略，确保每个模块提交前对应测试通过�?