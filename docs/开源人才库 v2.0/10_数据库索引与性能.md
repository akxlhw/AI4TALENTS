# 开源人才子系统 - 数据库索引与性能设计 (v2.0.0)

**文档编号**�?0  
**版本**：v2.0.0  
**状�?*：设计稿  
**适用范围**：智能人才库—开源人才子系统 V2.0.0

---

## 1. 设计目标

为开源人才子系统 12 张核心表设计高性能索引策略，确保：
- 搜索/筛选查询响应时�?< 500ms（P95�?- 详情页关联查询响应时�?< 200ms
- 采集流水线批量写入吞吐量 > 100 �?�?- pgvector 向量相似度搜索响应时�?< 2s

---

## 2. 数据量级估算

### 2.1 初始数据规模

基于 35 个预置仓库、每仓库 30 名贡献者的配置�?
| �?| 初始行数估算 | 单行大小估算 | 总大�?|
|----|-------------|-------------|--------|
| `os_repo_config` | 35 | ~500 bytes | ~17 KB |
| `os_developer` | 1,000 | ~2 KB | ~2 MB |
| `os_repository` | 30,000 | ~1 KB | ~30 MB |
| `os_contribution` | 30,000 | ~200 bytes | ~6 MB |
| `os_language_skill` | 5,000 | ~150 bytes | ~750 KB |
| `os_embedding` | 1,000 | ~6 KB (1536dim float32) | ~6 MB |
| `os_favourite` | 500 | ~100 bytes | ~50 KB |
| `os_talent_pool` | 50 | ~200 bytes | ~10 KB |
| `os_pool_member` | 1,000 | ~50 bytes | ~50 KB |
| `os_collect_task` | 100 | ~2 KB | ~200 KB |
| `os_raw_developer` | 1,000 | ~50 KB (json) | ~50 MB |
| `os_repo_mapping` | 100 | ~100 bytes | ~10 KB |

**初始总数据量**：约 95 MB（不含索引和 WAL�?
### 2.2 增长预测

| 场景 | 增长速率 | 1 年后估算 |
|------|----------|-----------|
| 仓库绑定扩展�?100 �?| +65 仓库 | 100 仓库 �?~3,000 开发�?|
| 每仓库贡献者扩展到 100 �?| 3× | ~9,000 开发�?|
| 新增数据源（GitLab 等） | +1 子系�?| 数据量翻�?|

**设计预留**：索引策略应支撑 10 万开发者�?00 万仓库的规模�?
---

## 3. 索引设计详单

### 3.1 os_repo_config

| 索引�?| 字段 | 类型 | 说明 |
|--------|------|------|------|
| `pk_os_repo_config` | `repo_config_id` | 主键 | |
| `uk_os_repo_config_full_name` | `repo_full_name` | 唯一 | 防止重复绑定 |
| `ix_os_repo_config_active_element` | (`is_active`, `tech_element`) | B-tree | 按技术领域筛选启用的仓库 |
| `ix_os_repo_config_collect` | (`is_active`, `collect_enabled`) | B-tree | 采集时快速定位目标仓�?|

**核心查询场景**�?```sql
-- 场景：采集时获取某技术领域的启用仓库
SELECT * FROM os_repo_config
WHERE is_active = TRUE AND collect_enabled = TRUE AND tech_element = 'ai';
-- 命中 ix_os_repo_config_collect + tech_element 条件
```

### 3.2 os_developer

| 索引�?| 字段 | 类型 | 说明 |
|--------|------|------|------|
| `pk_os_developer` | `developer_id` | 主键 | |
| `uk_os_developer_github_login` | `github_login` | 唯一 | GitHub 用户名唯一 |
| `ix_os_developer_visible` | (`is_visible`) | B-tree 部分索引 | �?`is_visible = TRUE` |
| `ix_os_developer_stars_desc` | (`total_stars_received` DESC) | B-tree | �?Stars 排序 |
| `ix_os_developer_tech_tags_gin` | `tech_tags` | GIN | JSONB 数组包含查询 |
| `ix_os_developer_langs_gin` | `primary_languages` | GIN | 编程语言筛�?|
| `ix_os_developer_company` | `company` | B-tree | 公司过滤（ILIKE 走索引需 pattern 左固定）|
| `ix_os_developer_location` | `location` | B-tree | 地区过滤 |

> **注意**：当�?migration �?`tech_tags` �?`primary_languages` �?JSON 类型。若使用 PostgreSQL `jsonb` 类型，GIN 索引可加�?`@>` / `?|` 操作；若�?`json` 类型，需转换�?`jsonb` 才能�?GIN 索引�?
**核心查询场景**�?```sql
-- 场景 1：关键词搜索（ILIKE�?SELECT * FROM os_developer
WHERE is_visible = TRUE
  AND (name ILIKE '%pytorch%' OR bio ILIKE '%pytorch%' OR company ILIKE '%pytorch%')
ORDER BY total_stars_received DESC
LIMIT 20 OFFSET 0;
-- 命中 ix_os_developer_visible + 各字段条�?
-- 场景 2：技术领域筛�?SELECT * FROM os_developer
WHERE is_visible = TRUE AND tech_tags @> '["ai"]'
ORDER BY total_stars_received DESC;
-- 命中 ix_os_developer_tech_tags_gin + ix_os_developer_stars_desc

-- 场景 3：编程语言筛�?SELECT * FROM os_developer
WHERE is_visible = TRUE AND primary_languages @> '["Python"]'
ORDER BY total_stars_received DESC;
-- 命中 ix_os_developer_langs_gin
```

### 3.3 os_repository

| 索引�?| 字段 | 类型 | 说明 |
|--------|------|------|------|
| `pk_os_repository` | `repo_id` | 主键 | |
| `ix_os_repository_dev_id` | `developer_id` | B-tree | 按开发者查仓库列表 |
| `ix_os_repository_github_id` | `github_repo_id` | B-tree | GitHub 仓库 ID 查重 |
| `ix_os_repository_stars` | (`stars_count` DESC) | B-tree | �?Stars 排序 |
| `ix_os_repository_language` | `language` | B-tree | 按语言筛�?|

**核心查询场景**�?```sql
-- 场景：开发者详情页加载仓库列表
SELECT * FROM os_repository
WHERE developer_id = 123
ORDER BY stars_count DESC
LIMIT 50;
-- 命中 ix_os_repository_dev_id + ix_os_repository_stars
```

### 3.4 os_contribution

| 索引�?| 字段 | 类型 | 说明 |
|--------|------|------|------|
| `pk_os_contribution` | `contribution_id` | 主键 | |
| `ix_os_contribution_dev_repo` | (`developer_id`, `repo_id`) | B-tree 唯一 | 防止重复贡献记录 |
| `ix_os_contribution_dev_id` | `developer_id` | B-tree | 按开发者查贡献 |

### 3.5 os_language_skill

| 索引�?| 字段 | 类型 | 说明 |
|--------|------|------|------|
| `pk_os_language_skill` | `skill_id` | 主键 | |
| `ix_os_language_skill_dev_id` | `developer_id` | B-tree | 按开发者查语言技�?|
| `ix_os_language_skill_dev_lang` | (`developer_id`, `language`) | B-tree 唯一 | 每个开发者每种语言一条记�?|

### 3.6 os_embedding

| 索引�?| 字段 | 类型 | 说明 |
|--------|------|------|------|
| `pk_os_embedding` | `embedding_id` | 主键 | |
| `ix_os_embedding_dev_type` | (`developer_id`, `vector_type`) | B-tree 唯一 | 每个开发者每种类型一条嵌�?|
| `ix_os_embedding_hash` | `source_text_hash` | B-tree | 去重校验 |

**pgvector 专用索引（需单独迁移�?*�?
```sql
-- �?embedding 字段升级�?pgvector vector(1536) 类型后：
CREATE INDEX ix_os_embedding_vector ON os_embedding
USING ivfflat (embedding vector_cosine_ops)
WITH (lists = 100);
-- ivfflat 适合 10 万级向量，lists �?�?sqrt(行数)
```

> **当前状�?*：migration �?`embedding` �?`String` 类型，需后续创建专用迁移升级�?`vector` 类型并建�?ivfflat/hnsw 索引�?
### 3.7 os_favourite

| 索引�?| 字段 | 类型 | 说明 |
|--------|------|------|------|
| `pk_os_favourite` | `favourite_id` | 主键 | |
| `uk_os_favourite_user_dev` | (`user_id`, `developer_id`) | 唯一 | 防止重复收藏 |
| `ix_os_favourite_user_id` | `user_id` | B-tree | 按用户查收藏列表 |

### 3.8 os_talent_pool / os_pool_member

| 索引�?| 字段 | 类型 | 说明 |
|--------|------|------|------|
| `pk_os_talent_pool` | `pool_id` | 主键 | |
| `ix_os_talent_pool_owner` | `owner_user_id` | B-tree | 按所有者查人才�?|
| `pk_os_pool_member` | `pool_member_id` | 主键 | |
| `uk_os_pool_member_pool_dev` | (`pool_id`, `developer_id`) | 唯一 | 防止重复入池 |

### 3.9 os_collect_task

| 索引�?| 字段 | 类型 | 说明 |
|--------|------|------|------|
| `pk_os_collect_task` | `task_id` | 主键 | |
| `ix_os_collect_task_status` | `status` | B-tree | 按状态筛选（查询 running 任务）|
| `ix_os_collect_task_created_at` | `created_at` DESC | B-tree | 按时间倒序列任�?|

---

## 4. 查询性能优化策略

### 4.1 搜索查询优化

**问题**：关键词搜索使用多字�?`ILIKE`，难以全部走索引�?
**方案**�?1. **前缀匹配优先**：若用户输入 3 字符以上，使�?`name ILIKE 'pyt%'` 可走 `ix_os_developer_name` 索引（需额外添加 name 单列索引�?2. **全文搜索升级**：未来可引入 PostgreSQL `tsvector` 全文索引�?   ```sql
   ALTER TABLE os_developer ADD COLUMN search_vector tsvector
     GENERATED ALWAYS AS (
       coalesce(name, '') || ' ' ||
       coalesce(bio, '') || ' ' ||
       coalesce(company, '') || ' ' ||
       coalesce(location, '')
     ) STORED;
   CREATE INDEX ix_os_developer_search ON os_developer USING GIN(search_vector);
   ```
3. **结果集裁�?*：先�?`is_visible = TRUE` �?`tech_elements` 筛选缩小范围，再做 ILIKE

### 4.2 详情页关联查询优�?
**问题**：开发者详情页需要同时加载仓库、贡献、语言技能，产生多次往返�?
**方案**�?1. **并行查询**：后端使�?`asyncio.gather` 并行执行 3 个独立查�?2. **选择性加�?*：Tab 切换时按需加载（如默认只加载仓库列表，贡献和语言在切�?Tab 时加载）
3. **适度反规范化**：在 `os_developer` 中冗�?`primary_languages` �?`tech_tags`，避免频�?JOIN

### 4.3 采集流水线写入优�?
**问题**：Phase 6 需�?upsert 大量开发者、仓库、贡献记录�?
**方案**�?1. **批量 upsert**：使�?PostgreSQL `INSERT ... ON CONFLICT DO UPDATE`�?   ```python
   from sqlalchemy.dialects.postgresql import insert

   stmt = insert(OSDeveloper).values([...])
   stmt = stmt.on_conflict_do_update(
       index_elements=['github_login'],
       set_={
           'name': stmt.excluded.name,
           'bio': stmt.excluded.bio,
           'total_stars_received': stmt.excluded.total_stars_received,
           'updated_at': func.now(),
       }
   )
   await session.execute(stmt)
   ```
2. **分批 flush**：每 100 条执行一�?`session.flush()`，避免事务过�?3. **禁用触发�?*：批量写入期间若有无必要的触发器/监听，可临时禁用

### 4.4 向量搜索优化

**问题**：pgvector 相似度搜索在大规模数据下性能下降�?
**方案**�?1. **索引选择**�?   - 数据�?< 1 万：精确搜索（`ORDER BY embedding <=> query` 不走索引也可接受�?   - 数据�?1 �?~ 100 万：ivfflat 索引
   - 数据�?> 100 万：hnsw 索引（内存占用更高，但召回率和速度更优�?2. **预过�?+ 向量搜索**：先�?`tech_element` / `language` 过滤缩小候选集，再在子集上做向量搜�?3. **向量维度匹配**：确�?`EMBEDDING_DIMENSION` 环境变量�?pgvector 字段维度完全一�?
---

## 5. 迁移脚本索引清单

```sql
-- 048_add_os_performance_indexes.sql（示例，实际需根据 Alembic 编号调整�?
-- os_repo_config
CREATE UNIQUE INDEX uk_os_repo_config_full_name ON os_repo_config(repo_full_name);
CREATE INDEX ix_os_repo_config_active_element ON os_repo_config(is_active, tech_element);
CREATE INDEX ix_os_repo_config_collect ON os_repo_config(is_active, collect_enabled);

-- os_developer
CREATE UNIQUE INDEX uk_os_developer_github_login ON os_developer(github_login);
CREATE INDEX ix_os_developer_visible ON os_developer(is_visible) WHERE is_visible = TRUE;
CREATE INDEX ix_os_developer_stars_desc ON os_developer(total_stars_received DESC);
CREATE INDEX ix_os_developer_tech_tags_gin ON os_developer USING GIN(tech_tags);
CREATE INDEX ix_os_developer_langs_gin ON os_developer USING GIN(primary_languages);
CREATE INDEX ix_os_developer_company ON os_developer(company);
CREATE INDEX ix_os_developer_location ON os_developer(location);

-- os_repository
CREATE INDEX ix_os_repository_dev_id ON os_repository(developer_id);
CREATE INDEX ix_os_repository_github_id ON os_repository(github_repo_id);
CREATE INDEX ix_os_repository_stars ON os_repository(stars_count DESC);
CREATE INDEX ix_os_repository_language ON os_repository(language);

-- os_contribution
CREATE UNIQUE INDEX uk_os_contribution_dev_repo ON os_contribution(developer_id, repo_id);
CREATE INDEX ix_os_contribution_dev_id ON os_contribution(developer_id);

-- os_language_skill
CREATE INDEX ix_os_language_skill_dev_id ON os_language_skill(developer_id);
CREATE UNIQUE INDEX uk_os_language_skill_dev_lang ON os_language_skill(developer_id, language);

-- os_embedding
CREATE UNIQUE INDEX uk_os_embedding_dev_type ON os_embedding(developer_id, vector_type);
CREATE INDEX ix_os_embedding_hash ON os_embedding(source_text_hash);

-- os_favourite
CREATE UNIQUE INDEX uk_os_favourite_user_dev ON os_favourite(user_id, developer_id);
CREATE INDEX ix_os_favourite_user_id ON os_favourite(user_id);

-- os_pool_member
CREATE UNIQUE INDEX uk_os_pool_member_pool_dev ON os_pool_member(pool_id, developer_id);

-- os_collect_task
CREATE INDEX ix_os_collect_task_status ON os_collect_task(status);
CREATE INDEX ix_os_collect_task_created_at ON os_collect_task(created_at DESC);
```

---

## 6. 监控与诊�?
### 6.1 慢查询监�?
```sql
-- 启用 PostgreSQL 慢查询日志（postgresql.conf�?log_min_duration_statement = 500  -- 记录超过 500ms 的查�?```

### 6.2 关键查询 Explain 示例

```sql
-- 检查关键词搜索执行计划
EXPLAIN ANALYZE
SELECT * FROM os_developer
WHERE is_visible = TRUE
  AND (name ILIKE '%pytorch%' OR bio ILIKE '%pytorch%')
ORDER BY total_stars_received DESC
LIMIT 20;
-- 期望：Bitmap Heap Scan + Index Scan + �?Seq Scan
```

### 6.3 索引使用检�?
```sql
-- 检查索引是否被使用
SELECT schemaname, tablename, indexname, idx_scan, idx_tup_read, idx_tup_fetch
FROM pg_stat_user_indexes
WHERE schemaname = 'public' AND tablename LIKE 'os_%'
ORDER BY idx_scan DESC;
```

---

## 7. 文档结论

本文档为开源人才子系统提供了完整的数据库索引与性能设计�?- **数据量级**：初�?~95 MB，设计预�?10 万开发�?/ 100 万仓�?- **索引清单**�?2 张表�?20+ 个索引，覆盖主键、唯一、B-tree、GIN、部分索�?- **pgvector 规划**：当�?String 存储 �?后续迁移�?vector + ivfflat/hnsw 索引
- **查询优化**：搜索走 GIN + 部分索引，详情页并行查询，采集用批量 upsert
- **迁移脚本**：完整的 SQL 索引创建语句，可直接纳入 Alembic 迁移

实现后应使用 `EXPLAIN ANALYZE` 验证核心查询执行计划，确保无全表扫描�?