# 开放 API P1（鉴权体系 + 管理 + 行业迁移 + 各域只读查询）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立多 API Key + 域级读写 scope 的开放鉴权体系与管理面板，迁移行业旧 Key，并在 `/api/v1/open-api/*` 下提供五域只读查询契约。

**Architecture:** `shared_api_key` 表（哈希存储）+ `require_api_key(scope)` 共享依赖 + super_admin 管理 API 与前端 tab；五域各自新增 `api/open_api.py` 路由（仅 import 本域 Service + shared，遵守分层铁律）；限流中间件按 api_key_id 键控并输出 Prometheus 计数。

**Tech Stack:** FastAPI Depends + SQLAlchemy 2.0 async + Alembic（新迁移 065，当前 head 064）+ 自研 MetricsRegistry + Vitest/antd（管理 tab）

**设计文档:** `docs/superpowers/specs/2026-08-26-open-api-design.md`

---

### Task 1: shared_api_key 模型与迁移

**Files:**
- Create: `backend/app/domains/shared/models/api_key.py`
- Create: `backend/migrations/versions/065_add_shared_api_key.py`
- Modify: `backend/app/domains/shared/models/__init__.py`
- Modify: `backend/app/model_registry.py`

- [ ] **Step 1: 定义模型**

创建 `backend/app/domains/shared/models/api_key.py`：

```python
"""Open-API key model — hashed storage, per-domain read/write scopes."""

from sqlalchemy import Boolean, DateTime, Integer, String
from sqlalchemy.dialects.postgresql import JSONB
from sqlalchemy.orm import Mapped, mapped_column

from app.core.database import Base
from app.domains.shared.models.base import TimestampMixin


class ApiKey(Base, TimestampMixin):
    """API key for external tools / skills calling the open API."""

    __tablename__ = "shared_api_key"

    api_key_id = mapped_column(Integer, primary_key=True, index=True, autoincrement=True)
    key_name = mapped_column(String(100), nullable=False)
    # sha256 hex of the full key; plaintext is never stored
    key_hash = mapped_column(String(64), nullable=False, unique=True, index=True)
    # first 8 chars of the plaintext key, for list-page recognition only
    key_prefix = mapped_column(String(8), nullable=False)
    # e.g. ["academic:read", "industry:write"]
    scopes = mapped_column(JSONB, nullable=False, default=list)
    is_active = mapped_column(Boolean, default=True, nullable=False)
    # optional per-key rate limit override (requests/min); NULL = global default
    rate_limit_per_minute: Mapped[int | None] = mapped_column(Integer, nullable=True)
    expires_at: Mapped[str | None] = mapped_column(DateTime, nullable=True)
    last_used_at: Mapped[str | None] = mapped_column(DateTime, nullable=True)
    created_by = mapped_column(Integer, nullable=True)
```

注意：`TimestampMixin` 的导入与用法参照 `app/domains/shared/models/system_config.py`；`Mapped/mapped_column` 若与该文件既有风格（Column）不一致，改用与 `iam.py` 一致的写法（保持同文件族风格统一优先）。`expires_at/last_used_at` 用 `DateTime` 的 `Column(...)` 形式即可。

- [ ] **Step 2: 导出与注册**

`backend/app/domains/shared/models/__init__.py` 增加：

```python
from app.domains.shared.models.api_key import ApiKey

__all__ = [..., "ApiKey"]
```

`backend/app/model_registry.py` 在 shared 模型 import 区（SystemConfig 之后）加：

```python
from app.domains.shared.models.api_key import ApiKey
```

并在 `__all__` 加入 `"ApiKey"`。

- [ ] **Step 3: 迁移**

创建 `backend/migrations/versions/065_add_shared_api_key.py`（手写，模式参照 057 等近期迁移；静态检查要求 revision 注解）：

```python
"""Add shared_api_key table for the open API.

Revision ID: 065
Revises: 064
"""

from typing import Union

import sqlalchemy as sa
from alembic import op
from sqlalchemy.dialects.postgresql import JSONB

revision: str = "065"
down_revision: Union[str, None] = "064"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.create_table(
        "shared_api_key",
        sa.Column("api_key_id", sa.Integer(), autoincrement=True, nullable=False),
        sa.Column("key_name", sa.String(length=100), nullable=False),
        sa.Column("key_hash", sa.String(length=64), nullable=False),
        sa.Column("key_prefix", sa.String(length=8), nullable=False),
        sa.Column("scopes", JSONB(), nullable=False, server_default="[]"),
        sa.Column("is_active", sa.Boolean(), nullable=False, server_default=sa.text("true")),
        sa.Column("rate_limit_per_minute", sa.Integer(), nullable=True),
        sa.Column("expires_at", sa.DateTime(), nullable=True),
        sa.Column("last_used_at", sa.DateTime(), nullable=True),
        sa.Column("created_by", sa.Integer(), nullable=True),
        sa.Column("created_at", sa.DateTime(), server_default=sa.text("now()"), nullable=False),
        sa.Column("updated_at", sa.DateTime(), server_default=sa.text("now()"), nullable=False),
        sa.PrimaryKeyConstraint("api_key_id"),
        sa.UniqueConstraint("key_hash"),
    )
    op.create_index("ix_shared_api_key_hash", "shared_api_key", ["key_hash"])
    op.create_index("ix_shared_api_key_active", "shared_api_key", ["is_active"])


def downgrade() -> None:
    op.drop_index("ix_shared_api_key_active", table_name="shared_api_key")
    op.drop_index("ix_shared_api_key_hash", table_name="shared_api_key")
    op.drop_table("shared_api_key")
```

- [ ] **Step 4: 验证迁移与模型一致性**

Run: `cd backend && uv run alembic upgrade head && uv run alembic check`
Expected: 无错误（065 应用成功，无新的 autogenerate 差异）

- [ ] **Step 5: 提交**

```bash
git add backend/app/domains/shared/models/api_key.py backend/app/domains/shared/models/__init__.py backend/app/model_registry.py backend/migrations/versions/065_add_shared_api_key.py
git commit -m "feat(open-api): shared_api_key 模型与迁移 065"
```

---

### Task 2: ApiKeyService（生成/校验/管理）— TDD

**Files:**
- Create: `backend/app/domains/shared/services/api_key_service.py`
- Test: `backend/tests/domains/shared/test_api_key_service.py`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/domains/shared/test_api_key_service.py`：

```python
"""ApiKeyService behavior: generate/hash/verify/scope/revocation/expiry."""

from __future__ import annotations

from datetime import datetime, timedelta, timezone

import pytest
from sqlalchemy.ext.asyncio import AsyncSession

from app.domains.shared.services.api_key_service import ApiKeyService


def _utcnow_naive() -> datetime:
    return datetime.now(timezone.utc).replace(tzinfo=None)


@pytest.mark.asyncio
async def test_create_and_verify_roundtrip(test_session: AsyncSession) -> None:
    svc = ApiKeyService(test_session)
    created = await svc.create_key(
        key_name="洞察 Skill", scopes=["academic:read", "industry:write"], created_by=1
    )
    await test_session.commit()

    assert created["key"].startswith("ak_")
    assert len(created["key"]) == 46  # ak_ + 43 chars base62
    assert created["record"].key_prefix == created["key"][:8]
    assert created["record"].key_hash != created["key"]  # hashed, not plaintext

    verified = await svc.verify_key(created["key"])
    assert verified is not None
    assert verified.api_key_id == created["record"].api_key_id


@pytest.mark.asyncio
async def test_verify_rejects_unknown_and_revoked(test_session: AsyncSession) -> None:
    svc = ApiKeyService(test_session)
    assert await svc.verify_key("ak_unknown") is None

    created = await svc.create_key(key_name="x", scopes=["academic:read"], created_by=1)
    await test_session.commit()
    await svc.set_active(created["record"].api_key_id, False)
    await test_session.commit()
    assert await svc.verify_key(created["key"]) is None


@pytest.mark.asyncio
async def test_verify_rejects_expired(test_session: AsyncSession) -> None:
    svc = ApiKeyService(test_session)
    created = await svc.create_key(
        key_name="过期",
        scopes=["academic:read"],
        expires_at=_utcnow_naive() - timedelta(minutes=1),
    )
    await test_session.commit()
    assert await svc.verify_key(created["key"]) is None


@pytest.mark.asyncio
async def test_scope_check_and_last_used_touch(test_session: AsyncSession) -> None:
    svc = ApiKeyService(test_session)
    created = await svc.create_key(
        key_name="部分授权", scopes=["academic:read"], created_by=1
    )
    await test_session.commit()

    assert svc.has_scope(created["record"], "academic:read") is True
    assert svc.has_scope(created["record"], "industry:write") is False

    await svc.verify_key(created["key"])
    await test_session.commit()
    await test_session.refresh(created["record"])
    assert created["record"].last_used_at is not None
```

- [ ] **Step 2: 运行确认失败**

Run: `cd backend && uv run pytest tests/domains/shared/test_api_key_service.py -v`
Expected: FAIL — 模块不存在

- [ ] **Step 3: 实现 Service**

创建 `backend/app/domains/shared/services/api_key_service.py`：

```python
"""API key lifecycle: generation, verification, and management."""

from __future__ import annotations

import hashlib
import secrets
from datetime import datetime, timezone

from sqlalchemy import select, update
from sqlalchemy.ext.asyncio import AsyncSession

from app.domains.shared.models.api_key import ApiKey

_KEY_ALPHABET = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"
_KEY_RANDOM_LEN = 43


def _hash_key(plaintext: str) -> str:
    return hashlib.sha256(plaintext.encode("utf-8")).hexdigest()


def _now() -> datetime:
    return datetime.now(timezone.utc).replace(tzinfo=None)


class ApiKeyService:
    """CRUD + verification for open-API keys (hash-only storage)."""

    def __init__(self, session: AsyncSession) -> None:
        self.session = session

    async def create_key(
        self,
        key_name: str,
        scopes: list[str],
        created_by: int | None = None,
        rate_limit_per_minute: int | None = None,
        expires_at: datetime | None = None,
    ) -> dict:
        """Create a key. Returns {"record": ApiKey, "key": plaintext-once}."""
        plaintext = "ak_" + "".join(secrets.choice(_KEY_ALPHABET) for _ in range(_KEY_RANDOM_LEN))
        record = ApiKey(
            key_name=key_name,
            key_hash=_hash_key(plaintext),
            key_prefix=plaintext[:8],
            scopes=sorted(set(scopes)),
            rate_limit_per_minute=rate_limit_per_minute,
            expires_at=expires_at,
            created_by=created_by,
        )
        self.session.add(record)
        await self.session.flush()
        return {"record": record, "key": plaintext}

    async def verify_key(self, plaintext: str) -> ApiKey | None:
        """Return the active, unexpired key record, or None. Touches last_used_at."""
        result = await self.session.execute(
            select(ApiKey).where(ApiKey.key_hash == _hash_key(plaintext))
        )
        record = result.scalar_one_or_none()
        if record is None or not record.is_active:
            return None
        if record.expires_at is not None and record.expires_at <= _now():
            return None
        await self.session.execute(
            update(ApiKey)
            .where(ApiKey.api_key_id == record.api_key_id)
            .values(last_used_at=_now())
        )
        return record

    @staticmethod
    def has_scope(record: ApiKey, scope: str) -> bool:
        return scope in (record.scopes or [])

    async def set_active(self, api_key_id: int, is_active: bool) -> None:
        await self.session.execute(
            update(ApiKey).where(ApiKey.api_key_id == api_key_id).values(is_active=is_active)
        )

    async def list_keys(self) -> list[ApiKey]:
        result = await self.session.execute(
            select(ApiKey).order_by(ApiKey.api_key_id.desc())
        )
        return list(result.scalars().all())

    async def get_by_id(self, api_key_id: int) -> ApiKey | None:
        result = await self.session.execute(
            select(ApiKey).where(ApiKey.api_key_id == api_key_id)
        )
        return result.scalar_one_or_none()
```

- [ ] **Step 4: 运行确认通过**

Run: `cd backend && uv run pytest tests/domains/shared/test_api_key_service.py -v`
Expected: 4 PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/domains/shared/services/api_key_service.py backend/tests/domains/shared/test_api_key_service.py
git commit -m "feat(open-api): ApiKeyService 生成/校验/吊销/过期（哈希存储）"
```

---

### Task 3: require_api_key 依赖 — TDD

**Files:**
- Create: `backend/app/domains/shared/api/open_api_auth.py`
- Test: `backend/tests/domains/shared/test_open_api_auth.py`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/domains/shared/test_open_api_auth.py`：

```python
"""require_api_key dependency: 401/403/503 semantics and principal shape."""

from __future__ import annotations

import pytest
from fastapi import Depends, FastAPI, Header
from fastapi.testclient import TestClient
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.database import get_async_session
from app.domains.shared.api.open_api_auth import require_api_key
from app.domains.shared.services.api_key_service import ApiKeyService


@pytest.fixture
def app(test_session: AsyncSession) -> FastAPI:
    async def override_session():
        yield test_session

    a = FastAPI()
    a.dependency_overrides[get_async_session] = override_session

    @a.get("/probe")
    async def probe(principal: dict = Depends(require_api_key("academic:read"))):
        return principal

    return a


@pytest.mark.asyncio
async def test_missing_header_401(test_session: AsyncSession, app: FastAPI) -> None:
    with TestClient(app) as client:
        r = client.get("/probe")
    assert r.status_code == 401


@pytest.mark.asyncio
async def test_wrong_or_unknown_key_401(test_session: AsyncSession, app: FastAPI) -> None:
    with TestClient(app) as client:
        assert client.get("/probe", headers={"X-API-Key": "ak_wrong"}).status_code == 401


@pytest.mark.asyncio
async def test_scope_missing_403(test_session: AsyncSession, app: FastAPI) -> None:
    svc = ApiKeyService(test_session)
    created = await svc.create_key(key_name="只读行业", scopes=["industry:read"], created_by=1)
    await test_session.commit()
    with TestClient(app) as client:
        r = client.get("/probe", headers={"X-API-Key": created["key"]})
    assert r.status_code == 403
    assert "academic:read" in r.json()["detail"]


@pytest.mark.asyncio
async def test_valid_key_returns_principal(test_session: AsyncSession, app: FastAPI) -> None:
    svc = ApiKeyService(test_session)
    created = await svc.create_key(
        key_name="洞察", scopes=["academic:read", "industry:write"], created_by=1
    )
    await test_session.commit()
    with TestClient(app) as client:
        r = client.get("/probe", headers={"X-API-Key": created["key"]})
    assert r.status_code == 200
    body = r.json()
    assert body["role"] == "api_agent"
    assert body["key_name"] == "洞察"
    assert body["scopes"] == ["academic:read", "industry:write"]
```

- [ ] **Step 2: 运行确认失败**

Run: `cd backend && uv run pytest tests/domains/shared/test_open_api_auth.py -v`
Expected: FAIL — 模块不存在

- [ ] **Step 3: 实现依赖**

创建 `backend/app/domains/shared/api/open_api_auth.py`：

```python
"""Open-API authentication dependency (X-API-Key + scope check)."""

from __future__ import annotations

from fastapi import Depends, Header, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.database import get_async_session
from app.domains.shared.services.api_key_service import ApiKeyService


async def require_api_key(
    scope: str,
):
    """Build a dependency that verifies the X-API-Key header and requires ``scope``.

    The returned principal dict is attached to request.state by the rate-limit
    integration (api_key_id / rate_limit_per_minute fields included).
    """

    async def _dependency(
        api_key: str | None = Header(None, alias="X-API-Key"),
        session: AsyncSession = Depends(get_async_session),
    ) -> dict:
        if not api_key:
            raise HTTPException(status_code=401, detail="Missing X-API-Key header.")
        service = ApiKeyService(session)
        record = await service.verify_key(api_key)
        if record is None:
            raise HTTPException(status_code=401, detail="Invalid or revoked API key.")
        if not service.has_scope(record, scope):
            raise HTTPException(
                status_code=403,
                detail=f"API key lacks required scope: {scope}.",
            )
        await session.commit()  # persist last_used_at touch
        return {
            "role": "api_agent",
            "api_key_id": record.api_key_id,
            "key_name": record.key_name,
            "scopes": record.scopes or [],
            "rate_limit_per_minute": record.rate_limit_per_minute,
        }

    return _dependency
```

- [ ] **Step 4: 运行确认通过**

Run: `cd backend && uv run pytest tests/domains/shared/test_open_api_auth.py -v`
Expected: 4 PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/domains/shared/api/open_api_auth.py backend/tests/domains/shared/test_open_api_auth.py
git commit -m "feat(open-api): require_api_key 依赖（scope 校验 + principal 注入）"
```

---

### Task 4: 管理 API（super_admin CRUD）

**Files:**
- Create: `backend/app/domains/shared/api/api_keys.py`
- Modify: `backend/app/api_router.py`
- Create: `backend/app/domains/shared/schemas/api_key.py`
- Test: `backend/tests/domains/shared/test_api_keys_endpoint.py`

- [ ] **Step 1: 写 schema**

创建 `backend/app/domains/shared/schemas/api_key.py`：

```python
"""API key management schemas."""

from __future__ import annotations

from datetime import datetime

from pydantic import BaseModel, Field


class ApiKeyCreateRequest(BaseModel):
    key_name: str = Field(..., min_length=1, max_length=100, description="备注名")
    scopes: list[str] = Field(..., min_length=1, description="如 ['academic:read']")
    rate_limit_per_minute: int | None = Field(None, ge=1, le=10000)
    expires_at: datetime | None = None


class ApiKeyCreatedResponse(BaseModel):
    api_key_id: int
    key_name: str
    key_prefix: str
    scopes: list[str]
    is_active: bool
    expires_at: datetime | None
    plaintext_key: str = Field(..., description="明文 Key，仅此一次返回")


class ApiKeyListItem(BaseModel):
    api_key_id: int
    key_name: str
    key_prefix: str
    scopes: list[str]
    is_active: bool
    rate_limit_per_minute: int | None
    expires_at: datetime | None
    last_used_at: datetime | None
    created_at: datetime | None


class ApiKeySetActiveRequest(BaseModel):
    is_active: bool
```

- [ ] **Step 2: 写失败测试**

创建 `backend/tests/domains/shared/test_api_keys_endpoint.py`：

```python
"""API key management endpoints (super_admin only)."""

from __future__ import annotations

import pytest


@pytest.mark.asyncio
async def test_create_list_revoke_flow(super_admin_client) -> None:
    r = await super_admin_client.post(
        "/api/v1/api-keys",
        json={"key_name": "洞察 Skill", "scopes": ["academic:read", "industry:write"]},
    )
    assert r.status_code == 200
    created = r.json()
    assert created["plaintext_key"].startswith("ak_")
    assert created["key_prefix"] == created["plaintext_key"][:8]

    listed = (await super_admin_client.get("/api/v1/api-keys")).json()
    assert any(k["api_key_id"] == created["api_key_id"] for k in listed)
    item = next(k for k in listed if k["api_key_id"] == created["api_key_id"])
    assert "plaintext_key" not in item  # 列表永不返回明文
    assert set(item["scopes"]) == {"academic:read", "industry:write"}

    revoked = await super_admin_client.patch(
        f"/api/v1/api-keys/{created['api_key_id']}", json={"is_active": False}
    )
    assert revoked.status_code == 200
    listed2 = (await super_admin_client.get("/api/v1/api-keys")).json()
    item2 = next(k for k in listed2 if k["api_key_id"] == created["api_key_id"])
    assert item2["is_active"] is False


@pytest.mark.asyncio
async def test_key_management_forbidden_for_user(auth_client) -> None:
    r = await auth_client.get("/api/v1/api-keys")
    assert r.status_code == 403
```

- [ ] **Step 3: 运行确认失败**

Run: `cd backend && uv run pytest tests/domains/shared/test_api_keys_endpoint.py -v`
Expected: FAIL — 404（路由不存在）

- [ ] **Step 4: 实现端点**

创建 `backend/app/domains/shared/api/api_keys.py`：

```python
"""API key management endpoints (super_admin)."""

from __future__ import annotations

from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.database import get_async_session
from app.domains.shared.api.auth_deps import require_super_admin
from app.domains.shared.schemas.api_key import (
    ApiKeyCreateRequest,
    ApiKeyCreatedResponse,
    ApiKeyListItem,
    ApiKeySetActiveRequest,
)
from app.domains.shared.services.api_key_service import ApiKeyService
from app.domains.shared.services.audit_service import AuditService

router = APIRouter(prefix="/api-keys", tags=["API Keys"])


@router.post("", response_model=ApiKeyCreatedResponse, summary="创建 API Key（super_admin）")
async def create_api_key(
    req: ApiKeyCreateRequest,
    session: AsyncSession = Depends(get_async_session),
    admin: dict = Depends(require_super_admin),
) -> ApiKeyCreatedResponse:
    service = ApiKeyService(session)
    created = await service.create_key(
        key_name=req.key_name,
        scopes=req.scopes,
        created_by=admin["user_id"],
        rate_limit_per_minute=req.rate_limit_per_minute,
        expires_at=req.expires_at,
    )
    await session.commit()
    await AuditService.log_data_operation(
        user_id=admin["user_id"],
        operation="create",
        resource_type="api_key",
        resource_id=str(created["record"].api_key_id),
        status="success",
        detail={"key_name": req.key_name, "scopes": req.scopes},
    )
    r = created["record"]
    return ApiKeyCreatedResponse(
        api_key_id=r.api_key_id,
        key_name=r.key_name,
        key_prefix=r.key_prefix,
        scopes=r.scopes or [],
        is_active=r.is_active,
        expires_at=r.expires_at,
        plaintext_key=created["key"],
    )


@router.get("", response_model=list[ApiKeyListItem], summary="列出 API Key（super_admin）")
async def list_api_keys(
    session: AsyncSession = Depends(get_async_session),
    _admin: dict = Depends(require_super_admin),
) -> list[ApiKeyListItem]:
    records = await ApiKeyService(session).list_keys()
    return [
        ApiKeyListItem(
            api_key_id=r.api_key_id,
            key_name=r.key_name,
            key_prefix=r.key_prefix,
            scopes=r.scopes or [],
            is_active=r.is_active,
            rate_limit_per_minute=r.rate_limit_per_minute,
            expires_at=r.expires_at,
            last_used_at=r.last_used_at,
            created_at=r.created_at,
        )
        for r in records
    ]


@router.patch(
    "/{api_key_id}", response_model=ApiKeyListItem, summary="吊销/启用 API Key（super_admin）"
)
async def set_api_key_active(
    api_key_id: int,
    req: ApiKeySetActiveRequest,
    session: AsyncSession = Depends(get_async_session),
    admin: dict = Depends(require_super_admin),
) -> ApiKeyListItem:
    service = ApiKeyService(session)
    record = await service.get_by_id(api_key_id)
    if not record:
        raise HTTPException(status_code=404, detail="API key not found")
    await service.set_active(api_key_id, req.is_active)
    await session.commit()
    await session.refresh(record)
    await AuditService.log_data_operation(
        user_id=admin["user_id"],
        operation="update",
        resource_type="api_key",
        resource_id=str(api_key_id),
        status="success",
        detail={"is_active": req.is_active},
    )
    return ApiKeyListItem(
        api_key_id=record.api_key_id,
        key_name=record.key_name,
        key_prefix=record.key_prefix,
        scopes=record.scopes or [],
        is_active=record.is_active,
        rate_limit_per_minute=record.rate_limit_per_minute,
        expires_at=record.expires_at,
        last_used_at=record.last_used_at,
        created_at=record.created_at,
    )
```

挂载：`app/api_router.py` 在 system_config 挂载之后加：

```python
from app.domains.shared.api import api_keys
...
# Open API key management
api_router.include_router(api_keys.router)
```

注意：`super_admin_client`/`auth_client` fixture 来自 `tests/modules/open_source/conftest.py`——若 shared 测试树不可见，把测试文件放到 `backend/tests/modules/shared/` 并确认该 conftest 的 fixture 作用路径，或在本文件内按同样方式自建（复用 `tests/modules/open_source/conftest.py` 中 super_admin_client 的实现拷贝并适配）。

- [ ] **Step 5: 运行确认通过**

Run: `cd backend && uv run pytest tests/domains/shared/test_api_keys_endpoint.py -v`
Expected: 2 PASS

- [ ] **Step 6: 提交**

```bash
git add backend/app/domains/shared/api/api_keys.py backend/app/domains/shared/schemas/api_key.py backend/app/api_router.py backend/tests/domains/shared/test_api_keys_endpoint.py
git commit -m "feat(open-api): API Key 管理 CRUD（super_admin + 审计 + 明文一次性返回）"
```

---

### Task 5: 行业旧 Key 迁移 + 校验代理替换

**Files:**
- Modify: `backend/app/domains/industry/api/import_endpoint.py`
- Create: `backend/scripts/data/migrate_industry_api_key.py`
- Test: `backend/tests/domains/industry/test_industry_key_migration.py`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/domains/industry/test_industry_key_migration.py`：

```python
"""Legacy INDUSTRY_IMPORT_API_KEY migration into shared_api_key."""

from __future__ import annotations

import pytest
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.domains.shared.models.api_key import ApiKey
from app.domains.shared.services.config_service import ConfigService

_LEGACY = "legacy-secret-key-123"


@pytest.mark.asyncio
async def test_migration_creates_key_and_clears_sys_config(test_session: AsyncSession) -> None:
    await ConfigService(test_session).set_value("INDUSTRY_IMPORT_API_KEY", _LEGACY)
    await test_session.commit()

    from scripts.data.migrate_industry_api_key import migrate_industry_api_key

    await migrate_industry_api_key(test_session)

    records = (await test_session.execute(select(ApiKey))).scalars().all()
    assert len(records) == 1
    assert records[0].scopes == ["industry:write"]
    assert records[0].key_name == "行业导入(迁移)"
    legacy_value = await ConfigService(test_session).get_value(
        "INDUSTRY_IMPORT_API_KEY", use_cache=False
    )
    assert not legacy_value

    # 幂等：重跑不新增
    await migrate_industry_api_key(test_session)
    records2 = (await test_session.execute(select(ApiKey))).scalars().all()
    assert len(records2) == 1
```

- [ ] **Step 2: 运行确认失败**

Run: `cd backend && uv run pytest tests/domains/industry/test_industry_key_migration.py -v`
Expected: FAIL — 脚本模块不存在

- [ ] **Step 3: 实现迁移脚本**

创建 `backend/scripts/data/migrate_industry_api_key.py`：

```python
"""One-shot migration: INDUSTRY_IMPORT_API_KEY (sys_config) -> shared_api_key.

Idempotent; safe to run at deploy time or app startup. Run manually:
    cd backend && uv run python -m scripts.data.migrate_industry_api_key
"""

from __future__ import annotations

import asyncio
import hashlib
import logging

from sqlalchemy import delete, select
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.database import AsyncSessionLocal
from app.domains.shared.models.api_key import ApiKey
from app.domains.shared.models.system_config import SystemConfig
from app.domains.shared.services.config_service import ConfigService

logger = logging.getLogger(__name__)

_LEGACY_CONFIG_KEY = "INDUSTRY_IMPORT_API_KEY"
_MIGRATED_NAME = "行业导入(迁移)"


async def migrate_industry_api_key(session: AsyncSession) -> int:
    """Move the legacy industry import key into shared_api_key. Returns rows created."""
    legacy = await ConfigService(session).get_value(_LEGACY_CONFIG_KEY, use_cache=False)
    if not legacy:
        return 0

    key_hash = hashlib.sha256(legacy.encode("utf-8")).hexdigest()
    existing = await session.execute(
        select(ApiKey).where(ApiKey.key_hash == key_hash)
    )
    if existing.scalar_one_or_none() is None:
        session.add(
            ApiKey(
                key_name=_MIGRATED_NAME,
                key_hash=key_hash,
                key_prefix=legacy[:8],
                scopes=["industry:write"],
            )
        )
    await session.execute(
        delete(SystemConfig).where(SystemConfig.config_key == _LEGACY_CONFIG_KEY)
    )
    ConfigService._cache.pop(_LEGACY_CONFIG_KEY, None)
    await session.commit()
    logger.info("Migrated legacy industry import API key into shared_api_key")
    return 1


async def main() -> None:
    async with AsyncSessionLocal() as session:
        await migrate_industry_api_key(session)


if __name__ == "__main__":
    asyncio.run(main())
```

并在 `app/main.py` 的 lifespan startup（init_proxy_config 附近）追加一次幂等调用：

```python
    from app.domains.shared.services.config_service import ConfigService
    from scripts.data.migrate_industry_api_key import migrate_industry_api_key
    async with AsyncSessionLocal() as session:
        await migrate_industry_api_key(session)
```

（scripts 包在运行时可导入：`backend/scripts/__init__.py` 若不存在则创建空文件；若 lint 禁止 app 导入 scripts，则改为把迁移函数挪到 `app/domains/shared/services/api_key_service.py` 内作为 `migrate_legacy_industry_key` 类方法，scripts 文件仅作 CLI 壳——实现时二选一，保持 app 不依赖 scripts 目录。）

- [ ] **Step 4: 行业校验改走新依赖**

`import_endpoint.py` 中 `verify_industry_api_key` 改为：

```python
from app.domains.shared.api.open_api_auth import require_api_key

# 模块级构建一次（保持原函数签名/引用点不变）
_verify = require_api_key("industry:write")


async def verify_industry_api_key(
    api_key: str | None = Header(None, alias="X-API-Key"),
    session: AsyncSession = Depends(get_async_session),
) -> dict:
    """Delegate to the shared require_api_key(industry:write) dependency."""
    return await _verify(api_key=api_key, session=session)
```

外部 Header 与行为不变（401/403 语义略有细化：scope 不足变 403，原本 401——属于更准确的语义）。

- [ ] **Step 5: 运行测试（迁移 + 既有行业导入套件不回归）**

Run: `cd backend && uv run pytest tests/domains/industry/ -v && uv run pytest tests/modules/open_source/ -q`
Expected: 迁移测试 PASS；行业/开源域既有测试无回归

- [ ] **Step 6: 提交**

```bash
git add backend/scripts/data/migrate_industry_api_key.py backend/app/domains/industry/api/import_endpoint.py backend/app/main.py backend/tests/domains/industry/test_industry_key_migration.py
git commit -m "feat(open-api): 行业旧 Key 幂等迁移 + 校验改走 require_api_key"
```

---

### Task 6: open-api 五域只读查询端点

**Files:**
- Create: `backend/app/domains/shared/schemas/open_api.py`（公共 envelope）
- Create: `backend/app/domains/academic/api/open_api.py`
- Create: `backend/app/domains/open_source/api/open_api.py`（挂进该域 `api/open_source/__init__.py` 聚合 router）
- Create: `backend/app/domains/competition/api/open_api.py`（挂进 `api/__init__.py`）
- Create: `backend/app/domains/lab/api/open_api.py`（挂进 `api/__init__.py`）
- Create: `backend/app/domains/industry/api/open_api.py`（挂进 `api/__init__.py`）
- Test: `backend/tests/domains/shared/test_open_api_query_endpoints.py`

**统一契约**（所有列表端点）：

- 分页参数 `page`（默认 1）、`page_size`（默认 20，上限 100）
- 响应 envelope：`{"items": [...], "total": int, "page": int, "page_size": int}`
- 认证：`Depends(require_api_key("<domain>:read"))`
- 脱敏：响应剔除 email / social_links / blog_url / profile_url / orcid / extra_data

- [ ] **Step 1: 公共 schema**

创建 `backend/app/domains/shared/schemas/open_api.py`：

```python
"""Shared response envelopes for the open API."""

from __future__ import annotations

from typing import Generic, TypeVar

from pydantic import BaseModel, Field

T = TypeVar("T")


class OpenApiPage(BaseModel, Generic[T]):
    items: list[T] = Field(default_factory=list)
    total: int = 0
    page: int = 1
    page_size: int = 20
```

- [ ] **Step 2: 写失败测试（契约级，覆盖五域鉴权与形状）**

创建 `backend/tests/domains/shared/test_open_api_query_endpoints.py`：

```python
"""Open-API read endpoints: auth gate + envelope shape for all five domains."""

from __future__ import annotations

import pytest
from sqlalchemy.ext.asyncio import AsyncSession

from app.domains.shared.services.api_key_service import ApiKeyService

_ENDPOINTS = [
    ("/api/v1/open-api/academic/talents", "academic:read"),
    ("/api/v1/open-api/open-source/talents", "open_source:read"),
    ("/api/v1/open-api/competition/talents", "competition:read"),
    ("/api/v1/open-api/lab/talents", "lab:read"),
    ("/api/v1/open-api/industry/talents", "industry:read"),
]


@pytest.mark.asyncio
@pytest.mark.parametrize("url,scope", _ENDPOINTS)
async def test_read_endpoints_require_scope(client, test_session: AsyncSession, url, scope) -> None:
    # No key -> 401
    r = await client.get(url)
    assert r.status_code == 401

    # Key without the domain scope -> 403
    svc = ApiKeyService(test_session)
    created = await svc.create_key(key_name="无权限", scopes=["other:read"], created_by=1)
    await test_session.commit()
    r = await client.get(url, headers={"X-API-Key": created["key"]})
    assert r.status_code == 403


@pytest.mark.asyncio
async def test_academic_talents_envelope(client, test_session: AsyncSession) -> None:
    svc = ApiKeyService(test_session)
    created = await svc.create_key(key_name="学术读", scopes=["academic:read"], created_by=1)
    await test_session.commit()
    r = await client.get(
        "/api/v1/open-api/academic/talents", headers={"X-API-Key": created["key"]}
    )
    assert r.status_code == 200
    body = r.json()
    assert set(body) >= {"items", "total", "page", "page_size"}
    for item in body["items"]:
        assert "orcid" not in item and "extra_data" not in item
```

- [ ] **Step 3: 运行确认失败**

Run: `cd backend && uv run pytest tests/domains/shared/test_open_api_query_endpoints.py -v`
Expected: FAIL — 404（路由不存在）

- [ ] **Step 4: 学术域端点（示例完整实现）**

创建 `backend/app/domains/academic/api/open_api.py`：

```python
"""Open-API read endpoints for the academic domain (scope: academic:read)."""

from __future__ import annotations

from fastapi import APIRouter, Depends, HTTPException, Query
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.database import get_async_session
from app.domains.academic.services.talent_service import TalentService
from app.domains.shared.api.open_api_auth import require_api_key
from app.domains.shared.schemas.open_api import OpenApiPage

router = APIRouter(prefix="/open-api/academic", tags=["Open API — Academic"])

_require = require_api_key("academic:read")

# 面向外部的精简字段（PII 脱敏：剔除 orcid/extra_data）
_LIST_FIELDS = (
    "talent_id", "name", "name_en", "current_title", "role_type",
    "school_id", "works_count", "cited_by_count", "h_index", "topic_tags",
)


def _strip(talents: list) -> list[dict]:
    items = []
    for t in talents:
        d = {
            f: getattr(t, f)
            for f in _LIST_FIELDS
            if getattr(t, f, None) is not None
        }
        items.append(d)
    return items


@router.get("/talents", summary="学术人才列表（关键词/技术领域/角色筛选）")
async def list_academic_talents(
    keyword: str | None = Query(None, description="姓名/关键词"),
    tech_domain_id: int | None = Query(None, description="技术领域 ID"),
    role_type: str | None = Query(None),
    min_citations: int | None = Query(None, ge=0),
    page: int = Query(1, ge=1),
    page_size: int = Query(20, ge=1, le=100),
    session: AsyncSession = Depends(get_async_session),
    _principal: dict = Depends(_require),
) -> OpenApiPage:
    service = TalentService(session)
    if keyword:
        talents, total = await service.search_talents_basic(
            keyword, page=page, page_size=page_size, role_type=role_type
        )
    else:
        talents, total = await service.get_talent_list(
            role_type=role_type,
            min_citations=min_citations,
            page=page,
            page_size=page_size,
        )
    return OpenApiPage(items=_strip(talents), total=total, page=page, page_size=page_size)


@router.get("/talents/{talent_id}", summary="学术人才详情")
async def get_academic_talent(
    talent_id: int,
    session: AsyncSession = Depends(get_async_session),
    _principal: dict = Depends(_require),
) -> dict:
    talent = await TalentService(session).get_talent_by_id(talent_id)
    if not talent:
        raise HTTPException(status_code=404, detail="Talent not found")
    return _strip([talent])[0]


@router.get("/stats", summary="学术域统计")
async def academic_stats(
    session: AsyncSession = Depends(get_async_session),
    _principal: dict = Depends(_require),
) -> dict:
    return await TalentService(session).get_statistics()
```

挂载：`app/api_router.py` academic import 区加 `open_api`，并 `api_router.include_router(open_api.router)`。

- [ ] **Step 5: 其余四域端点**

同模式创建（各自的筛选映射到已探明的 service 签名）：

**`backend/app/domains/open_source/api/open_api.py`**（prefix `/open-api/open-source`，scope `open_source:read`）：
- `/talents`：调 `OSDeveloperService(session).list_developers(q=keyword, tech_elements=..., languages=..., location=..., min_stars=..., sort_by=..., page, page_size)`；序列化 `OSDeveloper` 时剔除 `email/social_links/blog_url`
- `/talents/{developer_id}`：`get_developer_detail`，剔除同上 PII
- `/stats`：`get_stats()`
- 挂载：`open_source/api/open_source/__init__.py` 聚合 router 中 `include_router(open_api.router)`

**`backend/app/domains/competition/api/open_api.py`**（prefix `/open-api/competition`，scope `competition:read`）：
- `/talents`：`CompTalentService.list_talents(keyword=..., country_code=..., school=..., min_rating=..., rank_title=..., sort_by=..., page, page_size)`（无 PII，直接返回 Summary DTO）
- `/talents/{talent_id}`：`get_detail`
- `/stats`：`CompStatsService.get_overview()`
- 挂载：competition `api/__init__.py`

**`backend/app/domains/lab/api/open_api.py`**（prefix `/open-api/lab`，scope `lab:read`）：
- `/talents`：`LabTalentService.list_talents(keyword=..., parent_lab=..., research_area=..., role_type=..., page, page_size)`（列表 Summary 本身无 email 输出）
- `/talents/{talent_id}`：`get_talent_detail` 后剔除 `email/social_links`
- `/stats`：`LabStatsService.get_stats()`
- 挂载：lab `api/__init__.py`

**`backend/app/domains/industry/api/open_api.py`**（prefix `/open-api/industry`，scope `industry:read`）：
- `/talents`：`IndustryTalentService.list_talents(keyword=..., position_id=..., min_score=..., status=..., page, page_size)`（Summary 剔除 `profile_url`）
- `/talents/{talent_id}`：`get_talent_detail` 后剔除 `profile_url`
- `/stats`：`IndustryPositionService.list_positions()` 聚合为 `{total_positions, positions: [{position_id, title, candidate_count, avg_match_score}]}`
- 挂载：industry `api/__init__.py`

- [ ] **Step 6: 运行测试确认通过 + 架构检查**

Run: `cd backend && uv run pytest tests/domains/shared/test_open_api_query_endpoints.py -v && uv run python scripts/check_architecture.py`
Expected: 全 PASS；架构零违例

- [ ] **Step 7: 提交**

```bash
git add backend/app/domains/shared/schemas/open_api.py backend/app/domains/*/api/open_api.py backend/app/domains/open_source/api/open_source/__init__.py backend/app/domains/competition/api/__init__.py backend/app/domains/lab/api/__init__.py backend/app/domains/industry/api/__init__.py backend/app/api_router.py backend/tests/domains/shared/test_open_api_query_endpoints.py
git commit -m "feat(open-api): 五域只读查询端点（统一 envelope + PII 脱敏 + scope 门禁）"
```

---

### Task 7: 限流按 Key + Prometheus 计数

**Files:**
- Modify: `backend/app/middleware/rate_limit.py`
- Modify: `backend/app/domains/shared/api/open_api_auth.py`
- Test: `backend/tests/domains/shared/test_api_key_rate_limit.py`

- [ ] **Step 1: principal 写入 request.state**

`open_api_auth.py` 的 `_dependency` 增加注入（需要 Request）：

```python
from fastapi import Request
...
    async def _dependency(
        request: Request,
        api_key: str | None = Header(None, alias="X-API-Key"),
        session: AsyncSession = Depends(get_async_session),
    ) -> dict:
        ...  # 原校验逻辑
        request.state.api_key_id = record.api_key_id
        request.state.api_key_rate_limit = record.rate_limit_per_minute
        return {...}
```

- [ ] **Step 2: 限流中间件识别**

`rate_limit.py` dispatch 中取键逻辑改为（原 user_id 分支前插入）：

```python
        api_key_id = getattr(request.state, "api_key_id", None)
        per_key_limit = getattr(request.state, "api_key_rate_limit", None)
        if api_key_id:
            key = f"apikey:{api_key_id}"
        else:
            user_id = getattr(request.state, "user_id", None)
            ...
```

注意：BaseHTTPMiddleware 的 `call_next` 之后 request.state 才带上 FastAPI 依赖写入的属性——而限流判定在 call_next 之前。因此**不能依赖依赖注入写 state**：改为中间件内直接解析 `X-API-Key` 头的 sha256 前缀做键（`apikey-hash:{hash[:16]}`，无需查库），per-key 限流值在响应阶段补 `X-RateLimit-Limit` 头时读取。若 hash 未命中有效 Key，请求随后会被依赖层 401 拒绝，多算一次限流无害。实现以"中间件自解析头"为准，并删除 Step 1 的 state 写入（保留 principal 返回不变）。

- [ ] **Step 3: Prometheus 计数**

`app/core/metrics.py` 模块级注册：

```python
OPEN_API_REQUEST_COUNT = metrics.counter(
    "open_api_requests_total",
    "Open API requests by key prefix and path",
)
```

`open_api_auth.py` 校验成功后记录：

```python
from app.core.metrics import metrics

metrics.counter(
    "open_api_requests_total",
    labels={"key_prefix": record.key_prefix, "path": request.url.path},
).inc()
```

- [ ] **Step 4: 测试**

创建 `backend/tests/domains/shared/test_api_key_rate_limit.py`：

```python
"""Rate limiter keys open-api requests by api key, not IP."""

from __future__ import annotations

from app.middleware.rate_limit import RateLimiter


def test_limiter_accepts_apikey_prefixed_keys() -> None:
    limiter = RateLimiter(requests_per_minute=2)
    key = "apikey-hash:abcd1234"
    assert limiter.is_allowed(key)[0] is True
    assert limiter.is_allowed(key)[0] is True
    assert limiter.is_allowed(key)[0] is False
```

Run: `cd backend && uv run pytest tests/domains/shared/test_api_key_rate_limit.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/middleware/rate_limit.py backend/app/core/metrics.py backend/app/domains/shared/api/open_api_auth.py backend/tests/domains/shared/test_api_key_rate_limit.py
git commit -m "feat(open-api): 按 API Key 限流（中间件自解析头）+ Prometheus 计数"
```

---

### Task 8: 前端管理 Tab（系统配置 → API Key 管理）

**Files:**
- Create: `frontend/src/services/api/apiKeys.ts`
- Create: `frontend/src/pages/system-config/api-keys-tab.tsx`
- Modify: `frontend/src/pages/system-config/index.tsx`（或承载主/子 Tab 的文件，加入「API Key 管理」tab）
- Test: `frontend/src/pages/system-config/api-keys-tab.test.tsx`

- [ ] **Step 1: API 客户端**

创建 `frontend/src/services/api/apiKeys.ts`：

```typescript
import { apiClient } from './client'

export interface ApiKeyListItem {
  api_key_id: number
  key_name: string
  key_prefix: string
  scopes: string[]
  is_active: boolean
  rate_limit_per_minute?: number | null
  expires_at?: string | null
  last_used_at?: string | null
  created_at?: string | null
}

export interface ApiKeyCreated {
  api_key_id: number
  key_name: string
  key_prefix: string
  scopes: string[]
  plaintext_key: string
}

export const apiKeysApi = {
  list: () => apiClient.get<ApiKeyListItem[]>('/api-keys'),
  create: (data: { key_name: string; scopes: string[]; rate_limit_per_minute?: number }) =>
    apiClient.post<ApiKeyCreated>('/api-keys', data),
  setActive: (id: number, is_active: boolean) =>
    apiClient.patch<ApiKeyListItem>(`/api-keys/${id}`, { is_active }),
}
```

- [ ] **Step 2: Tab 组件**

创建 `frontend/src/pages/system-config/api-keys-tab.tsx`（结构参照现有 tab：Card + Table + 创建 Modal）：
- scope 勾选矩阵：五域（academic/open_source/competition/lab/industry）× 读/写，Checkbox.Group
- 创建成功后 Modal 内一次性展示明文 Key（`Typography.Text copyable` + 警示"关闭后不可再查看"）
- 列表列：前缀（`ak_xxxx…`）、备注名、scope 标签（Tag，领域色）、状态（Badge）、限流、最近使用（formatDBLocal）、操作（吊销/启用 Popconfirm）
- mock api 的组件冒烟测试：渲染列表行 + 创建按钮存在

（完整组件代码在实现时按上述结构编写，遵循既有 tab 的 antd 用法与 getErrorMessage 工具。）

- [ ] **Step 3: 注册 tab + 测试 + 验证**

在系统配置页主 Tab 列表加入 `{ key: 'api-keys', label: 'API Key 管理', children: <ApiKeysTab /> }`（super_admin 可见——沿用该页现有的权限判断方式）。

Run: `cd frontend && npx vitest run src/pages/system-config/api-keys-tab.test.tsx && npx tsc -b && npm run build`
Expected: 全过

- [ ] **Step 4: 提交**

```bash
git add frontend/src/services/api/apiKeys.ts frontend/src/pages/system-config/api-keys-tab.tsx frontend/src/pages/system-config/index.tsx frontend/src/pages/system-config/api-keys-tab.test.tsx
git commit -m "feat(open-api): API Key 管理面板（scope 矩阵 + 明文一次性展示 + 吊销）"
```

---

### Task 9: 全量回归、Agent 指南、推送

- [ ] **Step 1: 对接文档**

创建 `docs/open-api/01-agent-guide.md`：认证头说明、scope 列表、五域查询端点 curl 示例、错误码（401/403/429）、限流说明（沿用 v5.0.0/03 对接指南风格）。

- [ ] **Step 2: 后端全量验证**

Run: `cd backend && uv run pytest -q && uv run ruff check app tests && uv run black --check app tests && uv run python scripts/check_architecture.py && uv run python scripts/ops/mypy_gate.py`
Expected: 全绿（mypy 基线漂移则按惯例 --regenerate，总数不增）

- [ ] **Step 3: 手工验收（Swagger）**

打开 `http://localhost:8003/docs`：确认 "Open API — *" 分组与 "API Keys" 分组出现；创建一把全读 Key，用 curl 验证：

```bash
curl -H "X-API-Key: ak_..." "http://localhost:8003/api/v1/open-api/academic/talents?page_size=2"
```

Expected: 200 + envelope；无 Key 401；无 scope 403。

- [ ] **Step 4: 提交推送**

```bash
git add docs/open-api/01-agent-guide.md
git commit -m "docs(open-api): Agent 对接指南（认证/scope/端点/错误码）"
git push origin main
```

---

## Self-Review 结论

- **Spec 覆盖**：鉴权表（T1）/校验依赖（T3）/管理（T4+T8）/旧 Key 迁移（T5）/五域只读（T6）/限流+指标（T7）/文档（T9）；P2 项（竞赛/实验室导入端点、跨域统一搜索）不在本计划，待 P1 完成后另立计划
- **占位符**：Task 8 Step 2 组件为结构化描述而非全码（前端组件按既有 tab 模式实现，接口/行为约束已完整定义）；其余任务均含完整代码
- **类型一致性**：`require_api_key(scope)` 返回 `_dependency` 闭包、principal 含 `rate_limit_per_minute`；ApiKeyService 方法名在 T2/T4/T5 引用一致
